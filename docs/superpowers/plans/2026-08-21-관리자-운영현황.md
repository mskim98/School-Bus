# 관리자 운영 현황 API 구현 계획 (BG-21 · BG-22)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 또는 superpowers:executing-plans 로 태스크 단위 실행. 체크박스(`- [ ]`)로 추적한다.

**Goal:** 관리자 웹이 버스별 운행 현황(위치·탑승/미탑승/누락·경로)과 운영 이상 징후를 한 번에 볼 수 있는 백엔드 API 구축.

**Architecture:** 기존 조각(위치·명단·승하차기록·노선계획)을 **합성하는 조회 전용 모듈** `operations` 를 신설한다. 도메인 로직은 건드리지 않고 기존 query 서비스와 리포지토리를 재사용한다. 탑승 상태 판정은 **순수 함수**로 분리해 단위 테스트로 고정한다.

**Tech Stack:** Spring Boot 4.1 / Java 25 · 기존 CQRS query 계층

**Spec:** `docs/superpowers/specs/2026-08-21-관리자-운영현황-design.md`

## Global Constraints

- 패키지 루트 **`src.backend`**. 신규 모듈은 **`src.backend.operations`** 하위. Spring Boot 4.1 / Java 25.
- **조회 전용이다.** `command` 패키지를 만들지 않는다. 기존 엔티티·도메인 로직을 수정하지 않는다.
- service·repository 의 `spec`/`impl` 분리는 "구현이 바뀔 가능성"이 기준(`backend/docs/reference.md`). **단일 구현이면 분리하지 않는다.**
- 인가는 기존 **`@CanMonitorOperations`** 재사용 + **`TenantGuard.resolveTenantId`** 로 학원 격리. 새 역할·새 애너테이션을 만들지 않는다.
- **N+1 금지.** `bus/query/BusQueryService.java:53` 의 배치 조회(`findByAssignedBusIdInAndActiveTrue`) 패턴을 따른다.
- **지표·응답에 추정값이 섞이면 그 사실을 응답 필드나 주석에 명시한다**(정차 도달·지연 판정은 계획 ETA 기반 추정).
- 주석은 주변 코드와 같은 **서술형 문체**. 모호한 은유 금지, 증상·수치를 특정.
- **`git add -A` 금지.** `./gradlew clean` 금지. Docker 는 이미 기동돼 있으니 직접 기동·정지하지 않는다. **`docker compose down` 절대 금지.**
- 현재 전체 테스트 **288개 통과**. 회귀가 나면 그대로 보고한다.

---

## File Structure

**신규 — `backend/src/main/java/src/backend/operations/`**

| 파일 | 책임 |
|---|---|
| `domain/BoardingStatus.java` | 탑승 상태 enum 5종 |
| `domain/BoardingStatusResolver.java` | **순수 판정 로직**(기록 + 정차도달 → 상태). 스프링 빈 아님 |
| `query/OperationsQueryService.java` | 버스 현황 합성 조회 |
| `query/OperationsSummaryQueryService.java` | 운영 요약 조회 |
| `controller/OperationsController.java` | `/api/operations/buses` 2개(목록·상세) |
| `controller/OperationsSummaryController.java` | `/api/operations/summary` 1개 — **별도 파일로 분리해 Task 5 를 병렬 가능하게 함** |
| `dto/*.java` | 응답 DTO |

**수정 — 최소한으로**

| 파일 | 변경 |
|---|---|
| `drivesession/controller/DriveSessionController.java` · query | `status` 필터 추가(BG-7) |

---

## Task 1: 탑승 상태 판정 로직 (순수 함수)

**Files:**
- Create: `backend/src/main/java/src/backend/operations/domain/BoardingStatus.java`
- Create: `backend/src/main/java/src/backend/operations/domain/BoardingStatusResolver.java`
- Test: `backend/src/test/java/src/backend/operations/domain/BoardingStatusResolverTest.java`

**Interfaces:**
- Consumes: 없음(순수 로직)
- Produces:
  - `enum BoardingStatus { ALIGHTED, BOARDED, PENDING, UPCOMING, MISSED }`
  - `BoardingStatusResolver.resolve(List<RideType> recordedTypes, LocalDateTime stopReachedAt, LocalDateTime now, Duration missedThreshold) → BoardingStatus`
  - `BoardingStatusResolver.estimateStopReachedAt(LocalDateTime sessionStartedAt, long etaSeconds) → LocalDateTime`

**판정 규칙 (설계 §3)**

| 상태 | 조건 |
|---|---|
| `ALIGHTED` | `ALIGHT` 또는 `HANDOVER` 기록 존재 |
| `BOARDED` | `BOARD` 기록 존재, 하차 기록 부재 |
| `UPCOMING` | 기록 부재 + 정차 미도달(`now < stopReachedAt`) **또는 세션 미시작(`stopReachedAt == null`)** |
| `PENDING` | 기록 부재 + 도달, 경과 < 임계 |
| `MISSED` | 기록 부재 + 도달, 경과 ≥ 임계 |

- [ ] **Step 1: 실패하는 테스트 작성**

```java
package src.backend.operations.domain;

import static org.assertj.core.api.Assertions.assertThat;

import java.time.Duration;
import java.time.LocalDateTime;
import java.util.List;

import org.junit.jupiter.api.Test;

import src.backend.rideevent.entity.RideType;

/**
 * 탑승 상태 판정 단위 테스트. 시각을 인자로 받는 순수 함수라 대기 없이 검증한다.
 * 임계값은 미승차 알림과 같은 기준(10분)을 쓴다 — 화면과 알림이 다른 기준으로 "누락"을 말하면 안 된다.
 */
class BoardingStatusResolverTest {

    private static final Duration MISSED = Duration.ofMinutes(10);
    private static final LocalDateTime NOW = LocalDateTime.of(2026, 8, 21, 8, 30);

    @Test
    void alight_beats_board() {
        assertThat(BoardingStatusResolver.resolve(
                List.of(RideType.BOARD, RideType.ALIGHT), NOW.minusMinutes(30), NOW, MISSED))
                .isEqualTo(BoardingStatus.ALIGHTED);
    }

    @Test
    void handover_is_also_alighted() {
        assertThat(BoardingStatusResolver.resolve(
                List.of(RideType.BOARD, RideType.HANDOVER), NOW.minusMinutes(30), NOW, MISSED))
                .isEqualTo(BoardingStatus.ALIGHTED);
    }

    @Test
    void board_only_is_boarded() {
        assertThat(BoardingStatusResolver.resolve(
                List.of(RideType.BOARD), NOW.minusMinutes(30), NOW, MISSED))
                .isEqualTo(BoardingStatus.BOARDED);
    }

    /** 세션이 아직 시작 안 됐으면 도달 시각을 계산할 근거가 없다 — 전원 예정이다. */
    @Test
    void noRecord_beforeSessionStart_isUpcoming() {
        assertThat(BoardingStatusResolver.resolve(List.of(), null, NOW, MISSED))
                .isEqualTo(BoardingStatus.UPCOMING);
    }

    @Test
    void noRecord_stopNotReached_isUpcoming() {
        assertThat(BoardingStatusResolver.resolve(List.of(), NOW.plusMinutes(5), NOW, MISSED))
                .isEqualTo(BoardingStatus.UPCOMING);
    }

    @Test
    void noRecord_justReached_isPending() {
        assertThat(BoardingStatusResolver.resolve(List.of(), NOW.minusMinutes(3), NOW, MISSED))
                .isEqualTo(BoardingStatus.PENDING);
    }

    /** 이 경계가 화면의 "누락" 표시를 가른다 — 알림(미승차 10분)과 같은 기준이어야 한다. */
    @Test
    void noRecord_pastThreshold_isMissed() {
        assertThat(BoardingStatusResolver.resolve(List.of(), NOW.minusMinutes(10), NOW, MISSED))
                .isEqualTo(BoardingStatus.MISSED);
    }

    @Test
    void noRecord_exactlyAtThresholdBoundary_isMissed() {
        assertThat(BoardingStatusResolver.resolve(List.of(), NOW.minusMinutes(10).minusSeconds(1), NOW, MISSED))
                .isEqualTo(BoardingStatus.MISSED);
    }

    @Test
    void estimateStopReachedAt_addsEtaToSessionStart() {
        assertThat(BoardingStatusResolver.estimateStopReachedAt(NOW, 300L))
                .isEqualTo(NOW.plusMinutes(5));
    }

    /** 세션 미시작이면 도달 시각을 추정할 수 없다. */
    @Test
    void estimateStopReachedAt_withoutSessionStart_isNull() {
        assertThat(BoardingStatusResolver.estimateStopReachedAt(null, 300L)).isNull();
    }
}
```

- [ ] **Step 2: 테스트 실패 확인** — `cd backend && ./gradlew test --tests '*BoardingStatusResolverTest*'` → 컴파일 실패
- [ ] **Step 3: 구현** — `BoardingStatus` enum + `BoardingStatusResolver`(private 생성자, static 메서드만). **`estimateStopReachedAt` 이 추정임을 javadoc 에 명시**하고 그 한계(실제 정차 도달 기록이 부재해 계획 ETA 를 근거로 삼으며, 지연 운행 시 실제보다 이르게 도달로 판정)를 적는다.
- [ ] **Step 4: 테스트 통과 확인** — 10개 통과
- [ ] **Step 5: 전체 회귀** — `./gradlew test` (288 + 10)
- [ ] **Step 6: 커밋**

```bash
git add backend/src/main/java/src/backend/operations/domain/ \
        backend/src/test/java/src/backend/operations/domain/
git commit -m "feat(operations): 탑승 상태 5단계 판정 로직 (BG-21)"
```

---

## Task 2: 진행 중 운행 세션 조회 (BG-7)

**Files:**
- Modify: `backend/src/main/java/src/backend/drivesession/controller/DriveSessionController.java`
- Modify: `backend/src/main/java/src/backend/drivesession/query/DriveSessionQueryService.java`
- Modify: `backend/src/main/java/src/backend/drivesession/repository/spec/DriveSessionRepository.java`
- Test: 기존 drivesession 테스트에 추가

**왜 필요한가:** 지금은 기사·관리자 모두 **전체 운행 이력을 받아 앱이 진행 중인 것을 골라낸다**(`DriveSessionController.java:91~104` 주석이 자체 명시). 이력이 쌓일수록 응답이 커진다. Task 3·4 가 이 조회를 쓴다.

**Interfaces:**
- Produces: `GET /api/drive-sessions/bus/{busId}?status=IN_PROGRESS` · `GET /api/drive-sessions?tenantId=&status=`
  - `status` 생략 시 **기존 동작 그대로**(전체 이력) — 프론트 호환을 깨지 않는다

- [ ] **Step 1: 실패하는 테스트 작성** — `status=IN_PROGRESS` 로 조회 시 진행 중만 반환 · 생략 시 전체 반환(하위호환)
- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현** — 리포지토리에 상태 필터 메서드 추가, 쿼리 서비스가 `status` 가 null 이면 기존 경로를 탄다
- [ ] **Step 4·5: 테스트 통과 + 전체 회귀**
- [ ] **Step 6: 커밋** — `feat(drivesession): 진행 중 세션 상태 필터 추가 (BG-7)`

---

## Task 3: 버스별 운행 현황 목록 API

**Files:**
- Create: `operations/dto/BusOperationSummary.java` · `operations/query/OperationsQueryService.java` · `operations/controller/OperationsController.java`
- Test: `backend/src/test/java/src/backend/operations/query/OperationsQueryServiceTest.java`

**Interfaces:**
- Consumes: Task 1 의 `BoardingStatusResolver`, Task 2 의 상태 필터
- Produces: `GET /api/operations/buses?tenantId=` → 설계 §4.1 의 응답

```
busId · busName · direction · sessionStatus
location { lat · lng · recordedAt · origin · stale }
counts   { total · boarded · alighted · pending · upcoming · missed }
```

**핵심 주의**
- `location.stale` — 좌표 TTL(15초) 만료를 **좌표 부재와 구분**한다. 조회 결과가 empty 면 `stale=true`.
- **N+1 금지** — 버스 목록·학생·당일 승하차기록을 **각각 한 번씩** 조회해 메모리에서 분배한다.
- 인가는 `@CanMonitorOperations` + `TenantGuard`.

- [ ] **Step 1~6:** 테스트 먼저 → 실패 확인 → 구현 → 통과 → 전체 회귀 → 커밋
  - 테스트에 **N+1 검증**을 넣어라: 버스 2대·학생 4명으로 리포지토리 호출 횟수가 버스 수에 비례하지 않음을 mock 호출 횟수로 고정한다.

---

## Task 4: 버스 1대 상세 API

**Files:**
- Create: `operations/dto/BusOperationDetail.java`
- Modify: `operations/query/OperationsQueryService.java` · `operations/controller/OperationsController.java`
- Test: 기존 `OperationsQueryServiceTest` 에 추가

**Interfaces:**
- Produces: `GET /api/operations/buses/{busId}` → 설계 §4.2

```
bus · session · routePlan { version · polyline · stops[ seq · name · lat · lng · etaSeconds · reachedAt ] }
students[ studentId · name · status · stopSeq · stopName · recordedAt · guardians[] ]
```

**반드시 담을 것**
- **정류장 이름**(`stops[].name`) — 기존 `RoutePlanResponse` 에 없어 화면이 "N번 정차"로만 표시하는 갭(`PRODUCT_SPEC` §12 **F10**)을 함께 해소한다.
- **보호자 연락처**(`students[].guardians[]`) — 설계 §5.1. **누락을 발견해도 연락 수단이 없으면 화면이 무용하다.**
- `stops[].reachedAt` 은 **추정값**이다. 필드명이나 주석으로 추정임을 드러내라.

- [ ] **Step 1~6:** 같은 절차

---

## Task 5: 운영 요약 API

**Files:**
- Create: `operations/dto/OperationsSummary.java` · `operations/query/OperationsSummaryQueryService.java`
- Modify: `operations/controller/OperationsController.java`
- Test: `backend/src/test/java/src/backend/operations/query/OperationsSummaryQueryServiceTest.java`

**Interfaces:**
- Produces: `GET /api/operations/summary?tenantId=` → 설계 §5 + **§5.1**

```
today        { busesTotal · running · completed · notStarted · byDirection }
pending      { sosOpen · attendancePending · scheduleChangePending }
dispatch     { overCapacityBuses[] · studentsWithoutCoords[] · busesWithoutCrew[] · unpublishedPlans[] · crewAssignment[] }
compliance   { insuranceExpired[] · insuranceExpiring[] }        ← §5.1
notifications{ today[] }                                          ← §5.1
anomalies    { staleLocationBuses[] · longRunningSessions[] · delayedBuses[] }
```

**항목별 근거 (설계 §5 표를 그대로 따른다)**
- `compliance` — `Bus.insuranceExpiry`. **만료됨 / 30일 이내 임박** 두 단계로 나눈다.
  ⚠ 실측(2026-08-21): 시드의 1호차가 `2026-08-15` 만료로 이미 만료 상태다. **테스트에서 이 구분이 실제로 갈리는지 확인해라.**
- `notifications.today` — `NotificationLogRepository.findByTenantIdOrderByCreatedAtDesc` 를 당일로 좁힌다.
- `dispatch.busesWithoutCrew` — **선탑자 미배정이면 승하차 기록 자체가 불가**(불변조건 I-1)하므로 기사 미배정과 구분해 표기한다.
- `anomalies.delayedBuses` — ⚠ **정확도가 낮다.** 계획 ETA 기반 추정이며 실적 기록이 약하다. **응답 필드나 주석에 추정임을 명시해라.**

- [ ] **Step 1~6:** 같은 절차. 각 항목마다 최소 1개 테스트.

---

## Task 6: 문서 갱신

- [ ] `docs/API_SPEC.md` 에 `/api/operations/*` 3개 엔드포인트 추가(권한·테넌트 격리·구현 상태 표기 포함)
- [ ] `docs/PRODUCT_SPEC.md` §12 에서 **F10**(정류장 이름 부재) 해소 표기 — Task 4 가 해소했다
- [ ] `backend/docs/plans/2026-08-21-백엔드-MVP-갭-보완.md` §4 에서 **BG-7·BG-21·BG-22** 상태 갱신
- [ ] ⚠ **프론트 화면은 이 계획의 범위 밖이다.** "관리자 웹에서 볼 수 있다"고 적지 마라 — API 만 생겼고 화면은 아직 없다.
- [ ] 커밋

---

## 실행 순서

```
[1차 병렬]  Task 1 (판정 로직)  ‖  Task 2 (BG-7)
                 ↓                    ↓
[2차 병렬]  Task 3 (목록)  ‖  Task 5 (요약)     ← 컨트롤러 파일이 분리돼 병렬 가능
                 ↓
[3차]       Task 4 (상세)   ← Task 3 과 같은 컨트롤러·쿼리서비스 수정, 순차 필수
                 ↓
[4차]       Task 6 (문서)
```

**병렬 상한은 파일 충돌이 아니라 Gradle 이다.** 이 저장소는 여러 에이전트가 동시에 `./gradlew test` 를 돌리면
가짜 실패(`FileNotFoundException at ClassPathResource`)가 난 전례가 있다. **동시 2개까지**로 두고,
각 에이전트에 "결과가 이상하면 `--rerun-tasks` 로 재확인하고 그 사실을 보고" 를 지시한다. `./gradlew clean` 은 금지.
