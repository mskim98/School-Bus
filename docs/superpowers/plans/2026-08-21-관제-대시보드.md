# 관제 대시보드 구현 계획 (Prometheus + Grafana)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로컬 개발환경에서 Prometheus + Grafana 로 API별 지표·서버 상태·데이터 계층·파이프라인 생존을 관제하는 대시보드 4장 구축.

**Architecture:** Spring Boot 에 `micrometer-registry-prometheus` 를 붙여 `/actuator/prometheus` 를 **compose 내부망에만** 노출하고, Prometheus 가 15초 주기로 스크레이프. 자동 지표(HTTP·JVM·HikariCP·Tomcat)는 의존성만으로 확보하고, 조용히 고장 나는 지점(스케줄러·Kafka 소비·위치 보고·STOMP·알림)은 `global/metrics` 의 얇은 계측 컴포넌트로 계측. Grafana 는 datasource·대시보드를 **파일 프로비저닝**으로 주입해 컨테이너 재생성에도 재현.

**Tech Stack:** Spring Boot 4.1 / Java 25 · Micrometer · Prometheus · Grafana · postgres_exporter · redis_exporter · node_exporter · Docker Compose

**Spec:** `docs/superpowers/specs/2026-08-21-관제-대시보드-design.md`

## Global Constraints

- 패키지 루트는 **`src.backend`**. 새 클래스는 이 하위에 둔다(`@SpringBootApplication` 컴포넌트 스캔 범위).
- service·repository 의 `spec`/`impl` 분리는 **"구현이 바뀔 가능성이 있는가"** 기준. 계측 컴포넌트는 단일 구현이므로 **분리하지 않는다**. 상세는 `backend/docs/reference.md`.
- **`/actuator` 의 외부 차단을 완화하지 않는다.** `infra/proxy/nginx.prod.conf:149` 의 `return 404` 유지. `management.endpoint.health.show-details: never` 유지.
- **지표에 버스 id·학생 id·사용자 id 태그를 넣지 않는다.** cardinality 폭증과 개인 식별 정보 유입을 함께 막는다(spec §4.2).
- **인스턴스 증설 금지 제약은 불변.** `@Scheduled` 중복·STOMP 세션 로컬 보관이 잔존(`docker-compose.prod.yml` 주석).
- 코드 주석은 **주변 코드와 같은 서술형 문체**를 쓴다. 모호한 은유 금지, 증상·수치를 특정한다.
- **이번 범위는 로컬(`docker-compose.yml`)뿐이다.** `docker-compose.prod.yml`·`nginx.prod.conf` 는 건드리지 않는다(spec §8 의 2단계).
- 테스트는 `cd backend && ./gradlew test` 로 확인한다. **`./gradlew clean` 금지** — 필요하면 `--rerun-tasks`.
- Docker 는 이미 기동돼 있다(postgres·redis·kafka). 필요 시 `docker compose up -d <서비스>` 로 추가하되 **`down -v` 금지**(named volume 삭제).

---

## File Structure

**신규 — 관측 모듈 (`backend/src/main/java/src/backend/observability/`)**

> **도메인 코드를 수정하지 않는다**(spec §5.3). 의존은 `observability` → 도메인 **단방향**이다.

| 파일 | 책임 |
|---|---|
| `metrics/SchedulerHealthMetrics.java` | 스케줄러별 마지막 성공 경과·실패 누적 보관 |
| `metrics/PipelineMetrics.java` | Kafka 소비 실패 · 버스 위치 수신 · 알림 발송 카운터 |
| `metrics/StompSessionMetrics.java` | STOMP 연결·해제 이벤트로 활성 세션 게이지 유지 |
| `aspect/ScheduledTaskMetricsAspect.java` | `@Scheduled` 메서드를 감싸 성공·실패 계측 |
| `aspect/KafkaListenerMetricsAspect.java` | `@KafkaListener` 메서드를 감싸 실패 계측 |
| `listener/BusLocationMetricsListener.java` | `BusLocationUpdatedEvent` 구독해 수신 계측 |
| `sender/MetricsNotificationSender.java` | `NotificationSender` 포트 구현체로 발송 계측 |

**신규 — 관측 스택 설정 (`infra/observability/`)**

| 파일 | 책임 |
|---|---|
| `prometheus/prometheus.yml` | 스크레이프 대상 5개 |
| `grafana/provisioning/datasources/prometheus.yml` | datasource 자동 등록 |
| `grafana/provisioning/dashboards/dashboards.yml` | 대시보드 폴더 자동 로드 |
| `grafana/dashboards/1-api.json` ~ `4-pipeline.json` | 대시보드 4장 |

**수정**

| 파일 | 변경 |
|---|---|
| `backend/build.gradle` | `micrometer-registry-prometheus`(Task 1) · `spring-boot-starter-aspectj`(Task 2 — ⚠ Boot 4.1 개명, `-aop` 아님) |
| `backend/src/main/resources/application.yml` | `prometheus` 노출 + 히스토그램 활성 |
| `docker-compose.yml` | 관측 컨테이너 5개 |

**도메인 파일은 한 줄도 수정하지 않는다.** 스케줄러 4개·Kafka 컨슈머 4개·`BusLocationCommandService`·
`NotificationCommandServiceImpl` 전부 미접촉이다. 리뷰에서 이 파일들이 diff 에 나타나면 결함으로 본다.

---

## Task 1: Prometheus 지표 노출

**Files:**
- Modify: `backend/build.gradle` (dependencies 블록)
- Modify: `backend/src/main/resources/application.yml:94~101` (management 블록)
- Test: `backend/src/test/java/src/backend/global/metrics/PrometheusEndpointTest.java` (Create)

**Interfaces:**
- Consumes: 없음
- Produces: `GET /actuator/prometheus` 가 200 + `text/plain` 으로 `http_server_requests_seconds_bucket` 등을 반환. 이후 모든 Task 의 지표가 이 엔드포인트로 나간다.

- [ ] **Step 1: 실패하는 테스트 작성**

`backend/src/test/java/src/backend/global/metrics/PrometheusEndpointTest.java`:

```java
package src.backend.global.metrics;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.MvcResult;
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;
import org.springframework.web.context.WebApplicationContext;

/**
 * 지표 노출 경로 테스트. Prometheus 가 스크레이프하는 유일한 통로이므로,
 * 이 엔드포인트가 막히면 대시보드 4장이 전부 빈 값이 된다.
 */
@SpringBootTest
class PrometheusEndpointTest {

    @Autowired
    private WebApplicationContext context;

    private MockMvc mockMvc() {
        return MockMvcBuilders.webAppContextSetup(context).build();
    }

    @Test
    void prometheusEndpoint_isExposed() throws Exception {
        MvcResult result = mockMvc()
                .perform(MockMvcRequestBuilders.get("/actuator/prometheus"))
                .andReturn();

        assertThat(result.getResponse().getStatus()).isEqualTo(200);
    }

    /** 백분위수는 기본 비활성이다 — 히스토그램 설정이 빠지면 p95·p99 패널이 전부 빈 값이 된다. */
    @Test
    void httpServerRequests_hasHistogramBuckets() throws Exception {
        mockMvc().perform(MockMvcRequestBuilders.get("/actuator/health")).andReturn();

        String body = mockMvc()
                .perform(MockMvcRequestBuilders.get("/actuator/prometheus").accept(MediaType.ALL))
                .andReturn().getResponse().getContentAsString();

        assertThat(body).contains("http_server_requests_seconds_bucket");
    }

    /**
     * URI 태그가 경로 패턴으로 묶이는지 확인하는 음성 대조.
     * id 별로 시계열이 갈라지면(`/api/buses/1`, `/api/buses/2` …) 시계열 수가 무한히 늘어
     * Prometheus 메모리가 급증한다. 패턴(`{id}`)으로 묶여야 한다.
     */
    @Test
    void uriTag_isPathPattern_notRawId() throws Exception {
        mockMvc().perform(MockMvcRequestBuilders.get("/api/buses/1")).andReturn();
        mockMvc().perform(MockMvcRequestBuilders.get("/api/buses/2")).andReturn();

        String body = mockMvc()
                .perform(MockMvcRequestBuilders.get("/actuator/prometheus").accept(MediaType.ALL))
                .andReturn().getResponse().getContentAsString();

        assertThat(body).doesNotContain("uri=\"/api/buses/1\"");
        assertThat(body).doesNotContain("uri=\"/api/buses/2\"");
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests '*PrometheusEndpointTest*'`
Expected: FAIL — `/actuator/prometheus` 가 404(노출 목록에 `health` 만 있음)

- [ ] **Step 3: 의존성 추가**

`backend/build.gradle` 의 actuator 줄 **바로 아래**에 추가:

```gradle
    // 지표 수집 — actuator 지표를 Prometheus 스크레이프 형식으로 노출한다.
    // 이 의존성 하나로 HTTP 요청·JVM·HikariCP·Tomcat·Kafka 컨슈머 지표가 자동 등록된다.
    implementation 'io.micrometer:micrometer-registry-prometheus'
```

- [ ] **Step 4: 노출·히스토그램 설정**

`backend/src/main/resources/application.yml` 의 `management` 블록을 아래로 교체(기존 주석 3줄은 유지하고 아래를 반영):

```yaml
management:
  endpoints:
    web:
      exposure:
        # prometheus 를 더하지만 외부 도달은 여전히 불가하다 —
        # nginx(infra/proxy/nginx.prod.conf)가 /actuator 를 404 로 막고,
        # Prometheus 는 compose 내부 네트워크에서 backend:8080 으로 직접 스크레이프한다.
        include: health,prometheus
  endpoint:
    health:
      show-details: never
  metrics:
    tags:
      # 지표에 붙는 공통 태그. 나중에 인스턴스가 늘면 여기서 구분한다.
      application: school-bus
    distribution:
      # 백분위수는 기본 비활성이다. 이 설정이 없으면 대시보드의 p95·p99 패널이 빈 값이 된다.
      percentiles-histogram:
        http.server.requests: true
      # 히스토그램 버킷 범위. 하한을 5ms 로 두는 이유는 대부분의 조회 API 가 그 아래로 내려가지 않아서고,
      # 상한 10s 는 외부 경로 API(OSRM·네이버 Directions) 호출이 섞인 배차 API 를 담기 위해서다.
      minimum-expected-value:
        http.server.requests: 5ms
      maximum-expected-value:
        http.server.requests: 10s
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests '*PrometheusEndpointTest*'`
Expected: PASS (3개)

- [ ] **Step 6: 전체 회귀 확인**

Run: `cd backend && ./gradlew test`
Expected: 기존 268개 + 신규 3개 전부 통과

- [ ] **Step 7: 커밋**

```bash
git add backend/build.gradle backend/src/main/resources/application.yml \
        backend/src/test/java/src/backend/global/metrics/PrometheusEndpointTest.java
git commit -m "feat(observability): /actuator/prometheus 노출 + 요청시간 히스토그램 활성 (MON-11)"
```

---

## Task 2: 스케줄러 생존 계측 (MON-8) — AOP 무침투

**Files:**
- Create: `backend/src/main/java/src/backend/observability/metrics/SchedulerHealthMetrics.java`
- Create: `backend/src/main/java/src/backend/observability/aspect/ScheduledTaskMetricsAspect.java`
- Modify: `backend/build.gradle` (`spring-boot-starter-aspectj` 추가 — ⚠ `-aop` 아님, 아래 Step 1 참조)
- Test: `backend/src/test/java/src/backend/observability/SchedulerHealthMetricsTest.java` (Create)
- Test: `backend/src/test/java/src/backend/observability/ScheduledTaskMetricsAspectTest.java` (Create)

**Interfaces:**
- Consumes: Task 1 의 `/actuator/prometheus`
- Produces:
  - `SchedulerHealthMetrics#recordSuccess(String scheduler)` · `#recordFailure(String scheduler)`
  - 지표 `schoolbus_scheduler_last_success_age_seconds{scheduler="..."}` (Gauge)
  - 지표 `schoolbus_scheduler_failures_total{scheduler="..."}` (Counter)
  - 태그 값은 **선언 클래스명에서 유도**한다: `ConnectionLossScheduler` → `connection-loss`,
    `LocationSimulationScheduler` → `location-simulation`, `ApproachNoShowScheduler` → `approach-no-show`,
    `SosEscalationScheduler` → `sos-escalation`. Task 8 대시보드는 `{{scheduler}}` legend 로 받으므로 값 목록을 하드코딩하지 않는다.

**⚠ 도메인 파일을 수정하지 않는다.** 스케줄러 4개 파일은 diff 에 나타나면 안 된다.

- [ ] **Step 1: AOP 의존성 추가**

`backend/build.gradle` 의 actuator·micrometer 줄 아래에 추가:

```gradle
    // @Scheduled·@KafkaListener 를 감싸 계측하기 위한 AOP.
    // 계측 호출을 도메인 메서드 안에 넣지 않으려고 쓴다 — observability 가 도메인을 참조할 뿐,
    // 도메인은 observability 를 모른다(설계 문서 §5.3).
    //
    // ⚠️ Boot 4.1 은 이 스타터를 spring-boot-starter-aspectj 로 개명했다. 구 좌표
    //    spring-boot-starter-aop 는 4.x BOM 에 없어 버전 해석부터 실패한다 —
    //    spring-boot-starter-web→webmvc, webflux→webclient 와 같은 계열의 개명이다.
    implementation 'org.springframework.boot:spring-boot-starter-aspectj'
```

- [ ] **Step 2: 실패하는 단위 테스트 작성**

`backend/src/test/java/src/backend/observability/SchedulerHealthMetricsTest.java`:

```java
package src.backend.observability;

import static org.assertj.core.api.Assertions.assertThat;

import java.util.concurrent.atomic.AtomicLong;

import org.junit.jupiter.api.Test;

import io.micrometer.core.instrument.simple.SimpleMeterRegistry;
import src.backend.observability.metrics.SchedulerHealthMetrics;

/** 시간 흐름은 주입한 시계로 조작해 대기 없이 검증한다. */
class SchedulerHealthMetricsTest {

    private final SimpleMeterRegistry registry = new SimpleMeterRegistry();
    private final AtomicLong now = new AtomicLong(1_000_000L);
    private final SchedulerHealthMetrics metrics = new SchedulerHealthMetrics(registry, now::get);

    @Test
    void recordSuccess_registersGaugeStartingAtZero() {
        metrics.recordSuccess("sos-escalation");
        assertThat(gauge("sos-escalation")).isEqualTo(0.0d);
    }

    /** 이 테스트가 계측의 존재 이유다 — 성공이 멈추면 경과가 늘어야 정지를 관측할 수 있다. */
    @Test
    void gauge_growsWhileSchedulerStopsSucceeding() {
        metrics.recordSuccess("sos-escalation");
        now.addAndGet(90_000L);
        assertThat(gauge("sos-escalation")).isEqualTo(90.0d);
    }

    @Test
    void recordSuccess_resetsAge() {
        metrics.recordSuccess("sos-escalation");
        now.addAndGet(90_000L);
        metrics.recordSuccess("sos-escalation");
        assertThat(gauge("sos-escalation")).isEqualTo(0.0d);
    }

    /** 실패는 성공 시각을 갱신하지 않는다 — 실패만 반복하면 경과가 계속 늘어야 한다. */
    @Test
    void recordFailure_countsAndDoesNotResetAge() {
        metrics.recordSuccess("connection-loss");
        now.addAndGet(30_000L);
        metrics.recordFailure("connection-loss");

        assertThat(gauge("connection-loss")).isEqualTo(30.0d);
        assertThat(registry.counter("schoolbus.scheduler.failures", "scheduler", "connection-loss").count())
                .isEqualTo(1.0d);
    }

    @Test
    void schedulers_areTrackedIndependently() {
        metrics.recordSuccess("sos-escalation");
        now.addAndGet(60_000L);
        metrics.recordSuccess("connection-loss");

        assertThat(gauge("sos-escalation")).isEqualTo(60.0d);
        assertThat(gauge("connection-loss")).isEqualTo(0.0d);
    }

    private double gauge(String scheduler) {
        return registry.get("schoolbus.scheduler.last.success.age")
                .tag("scheduler", scheduler).gauge().value();
    }
}
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests '*SchedulerHealthMetricsTest*'`
Expected: FAIL — 클래스 부재로 컴파일 실패

- [ ] **Step 4: 지표 보관 컴포넌트 구현**

`backend/src/main/java/src/backend/observability/metrics/SchedulerHealthMetrics.java`:

```java
package src.backend.observability.metrics;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;
import java.util.function.LongSupplier;

import org.springframework.stereotype.Component;

import io.micrometer.core.instrument.Gauge;
import io.micrometer.core.instrument.MeterRegistry;

/**
 * {@code @Scheduled} 작업이 마지막으로 성공한 뒤 흐른 시간과 실패 누적을 보관한다.
 *
 * <p>이 프로젝트의 스케줄러 4개는 예외를 잡아 로그만 남기고 계속 진행한다 — 판정 로직이 실패해도
 * 앱은 정상 기동 상태를 유지하고, 알림이 나가지 않는 것으로만 뒤늦게 드러난다. 특히
 * {@code SosEscalationScheduler} 가 멈추면 3분 미확인 SOS 가 에스컬레이션되지 않는다.
 * "마지막 성공 이후 경과"를 게이지로 두면 그 정지를 대시보드에서 볼 수 있다.
 *
 * <p>이 클래스는 값만 보관한다. 언제 호출할지는 {@code ScheduledTaskMetricsAspect} 가 정하며,
 * 스케줄러 코드는 이 계측의 존재를 모른다.
 *
 * <p>시계를 생성자로 주입받는 이유는 테스트에서 시간 흐름을 조작하기 위해서다.
 */
@Component
public class SchedulerHealthMetrics {

    private static final String AGE_METRIC = "schoolbus.scheduler.last.success.age";
    private static final String FAILURE_METRIC = "schoolbus.scheduler.failures";

    private final MeterRegistry registry;
    private final LongSupplier clockMillis;
    private final Map<String, AtomicLong> lastSuccessMillis = new ConcurrentHashMap<>();

    public SchedulerHealthMetrics(MeterRegistry registry) {
        this(registry, System::currentTimeMillis);
    }

    SchedulerHealthMetrics(MeterRegistry registry, LongSupplier clockMillis) {
        this.registry = registry;
        this.clockMillis = clockMillis;
    }

    /** 한 주기가 정상 완료됨. 경과 게이지가 0 으로 돌아간다. */
    public void recordSuccess(String scheduler) {
        anchor(scheduler).set(clockMillis.getAsLong());
    }

    /** 한 주기가 실패함. 실패 수만 올리고 성공 시각은 갱신하지 않는다 — 경과가 계속 늘어야 정지가 드러난다. */
    public void recordFailure(String scheduler) {
        anchor(scheduler);
        registry.counter(FAILURE_METRIC, "scheduler", scheduler).increment();
    }

    private AtomicLong anchor(String scheduler) {
        return lastSuccessMillis.computeIfAbsent(scheduler, name -> {
            AtomicLong value = new AtomicLong(clockMillis.getAsLong());
            Gauge.builder(AGE_METRIC, value, v -> (clockMillis.getAsLong() - v.get()) / 1000.0d)
                    .tag("scheduler", name)
                    .description("마지막 성공 이후 경과 시간(초). 값이 계속 늘면 그 스케줄러가 멈춘 것이다")
                    .baseUnit("seconds")
                    .register(registry);
            return value;
        });
    }
}
```

- [ ] **Step 5: 단위 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests '*SchedulerHealthMetricsTest*'`
Expected: PASS (5개)

- [ ] **Step 6: Aspect 가 실제로 걸리는지 보는 통합 테스트 작성**

⚠ **이 테스트가 이 Task 의 핵심이다.** AOP 프록시가 `@Scheduled` 빈에 실제로 적용되는지는 단위 테스트로 증명되지 않는다.
프록시가 안 걸리면 코드는 다 있는데 지표가 영원히 비어 있다.

`backend/src/test/java/src/backend/observability/ScheduledTaskMetricsAspectTest.java`:

```java
package src.backend.observability;

import static org.assertj.core.api.Assertions.assertThat;
import static org.awaitility.Awaitility.await;

import java.time.Duration;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import io.micrometer.core.instrument.MeterRegistry;

/**
 * AOP 프록시가 실제 {@code @Scheduled} 빈에 걸리는지 확인한다.
 * {@code LocationSimulationScheduler} 는 3초 주기라 짧게 기다리면 최소 한 번은 돈다.
 */
@SpringBootTest
class ScheduledTaskMetricsAspectTest {

    @Autowired
    private MeterRegistry registry;

    @Test
    void aspect_recordsGaugeForRealScheduledBean() {
        await().atMost(Duration.ofSeconds(20)).untilAsserted(() ->
                assertThat(registry.find("schoolbus.scheduler.last.success.age")
                        .tag("scheduler", "location-simulation").gauge())
                        .as("3초 주기 스케줄러가 돌았는데 게이지가 없으면 AOP 프록시가 걸리지 않은 것이다")
                        .isNotNull());
    }
}
```

`backend/build.gradle` 의 test 의존성에 추가:

```gradle
    testImplementation 'org.awaitility:awaitility'
```

- [ ] **Step 7: Aspect 구현**

`backend/src/main/java/src/backend/observability/aspect/ScheduledTaskMetricsAspect.java`:

```java
package src.backend.observability.aspect;

import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.Around;
import org.aspectj.lang.annotation.Aspect;
import org.springframework.stereotype.Component;

import src.backend.observability.metrics.SchedulerHealthMetrics;

/**
 * {@code @Scheduled} 메서드를 감싸 성공·실패를 계측한다.
 *
 * <p>계측 호출을 스케줄러 코드 안에 넣지 않는 이유는 두 가지다. 도메인이 관측 모듈을 참조하지 않게 하고,
 * 스케줄러가 4개에서 더 늘어도 계측이 자동으로 따라붙게 하려는 것이다.
 *
 * <p>태그 값은 선언 클래스명에서 유도한다 — {@code ConnectionLossScheduler} 는 {@code connection-loss} 다.
 * 이름 목록을 상수로 들고 있으면 스케줄러가 추가될 때 함께 고쳐야 하는데, 그 갱신을 잊으면 계측이 조용히 빠진다.
 */
@Aspect
@Component
public class ScheduledTaskMetricsAspect {

    private final SchedulerHealthMetrics metrics;

    public ScheduledTaskMetricsAspect(SchedulerHealthMetrics metrics) {
        this.metrics = metrics;
    }

    @Around("@annotation(org.springframework.scheduling.annotation.Scheduled)")
    public Object measure(ProceedingJoinPoint joinPoint) throws Throwable {
        String scheduler = schedulerName(joinPoint);
        try {
            Object result = joinPoint.proceed();
            metrics.recordSuccess(scheduler);
            return result;
        } catch (Throwable t) {
            // 계측만 하고 그대로 다시 던진다. 삼키면 스케줄러의 기존 예외 처리 동작이 바뀐다.
            metrics.recordFailure(scheduler);
            throw t;
        }
    }

    /** {@code ConnectionLossScheduler} → {@code connection-loss}. 접미사 Scheduler 를 떼고 케밥으로 바꾼다. */
    private String schedulerName(ProceedingJoinPoint joinPoint) {
        String simpleName = joinPoint.getTarget().getClass().getSimpleName();
        int proxyMarker = simpleName.indexOf("$$");
        if (proxyMarker > 0) {
            simpleName = simpleName.substring(0, proxyMarker);
        }
        String base = simpleName.endsWith("Scheduler")
                ? simpleName.substring(0, simpleName.length() - "Scheduler".length())
                : simpleName;
        return base.replaceAll("([a-z0-9])([A-Z])", "$1-$2").toLowerCase();
    }
}
```

⚠ **주의:** 이 프로젝트의 스케줄러 4개는 **자기 안에서 예외를 이미 잡는다.** 즉 정상 흐름에서 aspect 의
catch 는 거의 타지 않고 `recordSuccess` 만 돈다. 그래도 **경과 게이지는 정상 동작한다** — 내부 판정이 실패해
아무 일도 못 해도 메서드는 성공으로 반환되기 때문이다. **이 한계를 보고서에 적어라.** 실패 카운터를
제대로 채우려면 스케줄러가 예외를 던져야 하는데, 그건 도메인 수정이라 이번 범위 밖이다.

- [ ] **Step 8: 통합 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests '*ScheduledTaskMetricsAspectTest*'`
Expected: PASS. FAIL 이면 AOP 프록시가 안 걸린 것이므로 원인을 찾아 보고해라.

- [ ] **Step 9: 도메인 파일 미변경 확인**

Run: `git status --short backend/src/main/java/src/backend | grep -v observability`
Expected: **출력 없음.** 스케줄러 등 도메인 파일이 나오면 무침투 원칙 위반이므로 되돌려라.

- [ ] **Step 10: 전체 테스트 + 커밋**

Run: `cd backend && ./gradlew test`

```bash
git add backend/build.gradle \
        backend/src/main/java/src/backend/observability/ \
        backend/src/test/java/src/backend/observability/
git commit -m "feat(observability): 스케줄러 생존 계측 — AOP 로 도메인 수정 없이 (MON-8)"
```

---

## Task 3: 파이프라인 계측 (MON-9 + 위치 + 알림) — 무침투

**Files:**
- Create: `backend/src/main/java/src/backend/observability/metrics/PipelineMetrics.java`
- Create: `backend/src/main/java/src/backend/observability/aspect/KafkaListenerMetricsAspect.java`
- Create: `backend/src/main/java/src/backend/observability/listener/BusLocationMetricsListener.java`
- Create: `backend/src/main/java/src/backend/observability/sender/MetricsNotificationSender.java`
- Test: `backend/src/test/java/src/backend/observability/PipelineMetricsTest.java` (Create)

**Interfaces:**
- Consumes: Task 1 의 `/actuator/prometheus`
- Produces:
  - `PipelineMetrics#kafkaConsumeFailed(String consumer)` → `schoolbus_kafka_consume_failures_total{consumer="..."}`
  - `PipelineMetrics#busLocationReported(LocationOrigin origin)` → `schoolbus_bus_location_reports_total{origin="GPS"|"MOCK"}`
  - `PipelineMetrics#notificationSent(NotificationType type)` → `schoolbus_notifications_sent_total{type="..."}`
  - consumer 태그는 클래스명 유도: `BusLocationPushConsumer` → `bus-location-push`

**세 지점이 쓰는 확장점 (전부 이미 존재 — 도메인 수정 0줄):**

| 지표 | 확장점 | 근거 |
|---|---|---|
| Kafka 소비 실패 | `@Aspect` on `@KafkaListener` | 컨슈머 4개 |
| 버스 위치 수신 | `@EventListener(BusLocationUpdatedEvent)` | `BusLocationCommandService:55` 가 이미 발행 |
| 알림 발송 | `NotificationSender` 구현체 추가 | `NotificationCommandServiceImpl:29` 가 `List<NotificationSender>` 로 주입 |

- [ ] **Step 1: 실패하는 테스트 작성**

`backend/src/test/java/src/backend/observability/PipelineMetricsTest.java`:

```java
package src.backend.observability;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

import io.micrometer.core.instrument.simple.SimpleMeterRegistry;
import src.backend.location.dto.LocationOrigin;
import src.backend.notification.domain.NotificationType;
import src.backend.observability.metrics.PipelineMetrics;

class PipelineMetricsTest {

    private final SimpleMeterRegistry registry = new SimpleMeterRegistry();
    private final PipelineMetrics metrics = new PipelineMetrics(registry);

    @Test
    void kafkaConsumeFailed_countsPerConsumer() {
        metrics.kafkaConsumeFailed("location-push");
        metrics.kafkaConsumeFailed("location-push");
        metrics.kafkaConsumeFailed("routing-replan");

        assertThat(registry.counter("schoolbus.kafka.consume.failures", "consumer", "location-push").count())
                .isEqualTo(2.0d);
        assertThat(registry.counter("schoolbus.kafka.consume.failures", "consumer", "routing-replan").count())
                .isEqualTo(1.0d);
    }

    /** origin 태그로 실단말과 시뮬레이터를 구분한다 — 데모에서 버스가 움직이는 이유를 판별하는 근거다. */
    @Test
    void busLocationReported_separatesOrigin() {
        metrics.busLocationReported(LocationOrigin.GPS);
        metrics.busLocationReported(LocationOrigin.MOCK);
        metrics.busLocationReported(LocationOrigin.MOCK);

        assertThat(registry.counter("schoolbus.bus.location.reports", "origin", "GPS").count()).isEqualTo(1.0d);
        assertThat(registry.counter("schoolbus.bus.location.reports", "origin", "MOCK").count()).isEqualTo(2.0d);
    }

    @Test
    void notificationSent_countsPerType() {
        metrics.notificationSent(NotificationType.SOS);
        assertThat(registry.counter("schoolbus.notifications.sent", "type", "SOS").count()).isEqualTo(1.0d);
    }

    /**
     * 식별자 태그 미유입 확인(음성 대조). 버스 id 로 태그를 붙이면 시계열이 버스 수만큼 갈라지고
     * 개인 식별 가능성도 생긴다 — 설계 §4.2 의 경계다.
     */
    @Test
    void busLocationCounter_hasNoIdentifierTag() {
        metrics.busLocationReported(LocationOrigin.GPS);

        assertThat(registry.get("schoolbus.bus.location.reports").counter().getId().getTags())
                .noneMatch(tag -> tag.getKey().toLowerCase().contains("bus")
                        || tag.getKey().toLowerCase().contains("student"));
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests '*PipelineMetricsTest*'`
Expected: FAIL — 클래스 부재

- [ ] **Step 3: PipelineMetrics 구현**

`backend/src/main/java/src/backend/observability/metrics/PipelineMetrics.java`:

```java
package src.backend.observability.metrics;

import org.springframework.stereotype.Component;

import io.micrometer.core.instrument.MeterRegistry;
import src.backend.location.dto.LocationOrigin;
import src.backend.notification.domain.NotificationType;

/**
 * 비동기 파이프라인이 살아 있는지를 계측한다 — Kafka 소비, 버스 위치 수신, 알림 발송 세 가지다.
 *
 * <p>세 지점 모두 실패해도 HTTP 응답에는 드러나지 않는다. Kafka 컨슈머는 예외를 삼키고 로그만 남긴
 * 선례가 있고(알림 저장이 CHECK 제약 위반으로 막혔는데 앱은 생존), 위치 보고가 끊기면 관제 지도에서
 * 버스가 사라질 뿐 오류가 나지 않는다.
 *
 * <p>태그는 <b>종류</b>만 붙이고 버스·학생 식별자는 붙이지 않는다 — 시계열이 대상 수만큼 갈라지는 것을
 * 막고, 개인 식별 정보가 지표에 섞이지 않게 한다.
 */
@Component
public class PipelineMetrics {

    private final MeterRegistry registry;

    public PipelineMetrics(MeterRegistry registry) {
        this.registry = registry;
    }

    /** Kafka 메시지 처리 중 예외 발생. 증가가 멈추지 않으면 그 컨슈머가 계속 실패하는 중이다. */
    public void kafkaConsumeFailed(String consumer) {
        registry.counter("schoolbus.kafka.consume.failures", "consumer", consumer).increment();
    }

    /** 버스 좌표 1건 수신. 증가가 멈추면 기사 앱 보고와 서버 시뮬레이터가 모두 정지한 것이다. */
    public void busLocationReported(LocationOrigin origin) {
        registry.counter("schoolbus.bus.location.reports", "origin", origin.name()).increment();
    }

    /** 알림 1건 발송. 증가가 멈추면 알림 파이프라인이 끊긴 것이다. */
    public void notificationSent(NotificationType type) {
        registry.counter("schoolbus.notifications.sent", "type", type.name()).increment();
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests '*PipelineMetricsTest*'`
Expected: PASS (4개)

- [ ] **Step 5: Kafka aspect 구현**

`backend/src/main/java/src/backend/observability/aspect/KafkaListenerMetricsAspect.java`:

```java
package src.backend.observability.aspect;

import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.Around;
import org.aspectj.lang.annotation.Aspect;
import org.springframework.stereotype.Component;

import src.backend.observability.metrics.PipelineMetrics;

/**
 * {@code @KafkaListener} 메서드를 감싸 소비 실패를 계측한다.
 *
 * <p>이 저장소에는 컨슈머가 예외를 삼켜 알림 저장이 조용히 막힌 선례가 있다 — 앱은 생존하고 로그만
 * 남아서 뒤늦게야 드러났다. 실패 건수를 지표로 두면 그 상태를 대시보드에서 볼 수 있다.
 *
 * <p>예외는 계측 후 그대로 다시 던진다. 삼키면 Kafka 재처리 동작이 바뀐다.
 */
@Aspect
@Component
public class KafkaListenerMetricsAspect {

    private final PipelineMetrics metrics;

    public KafkaListenerMetricsAspect(PipelineMetrics metrics) {
        this.metrics = metrics;
    }

    @Around("@annotation(org.springframework.kafka.annotation.KafkaListener)")
    public Object measure(ProceedingJoinPoint joinPoint) throws Throwable {
        try {
            return joinPoint.proceed();
        } catch (Throwable t) {
            metrics.kafkaConsumeFailed(consumerName(joinPoint));
            throw t;
        }
    }

    /** {@code BusLocationPushConsumer} → {@code bus-location-push}. 접미사 Consumer 를 떼고 케밥으로 바꾼다. */
    private String consumerName(ProceedingJoinPoint joinPoint) {
        String simpleName = joinPoint.getTarget().getClass().getSimpleName();
        int proxyMarker = simpleName.indexOf("$$");
        if (proxyMarker > 0) {
            simpleName = simpleName.substring(0, proxyMarker);
        }
        String base = simpleName.endsWith("Consumer")
                ? simpleName.substring(0, simpleName.length() - "Consumer".length())
                : simpleName;
        return base.replaceAll("([a-z0-9])([A-Z])", "$1-$2").toLowerCase();
    }
}
```

- [ ] **Step 6: 위치 수신 리스너 구현**

`backend/src/main/java/src/backend/observability/listener/BusLocationMetricsListener.java`:

```java
package src.backend.observability.listener;

import org.springframework.context.event.EventListener;
import org.springframework.stereotype.Component;

import src.backend.location.event.BusLocationUpdatedEvent;
import src.backend.observability.metrics.PipelineMetrics;

/**
 * 버스 좌표 수신을 계측한다. {@code BusLocationCommandService} 가 저장 직후 이미 발행하는 이벤트를
 * 구독할 뿐이라 도메인 코드에 계측 호출을 넣지 않는다.
 *
 * <p>Kafka 로 나가는 push 경로와 같은 이벤트를 쓰지만, 이쪽은 애플리케이션 내부 리스너라
 * Kafka 가 죽어도 수신 계측은 계속 동작한다.
 */
@Component
public class BusLocationMetricsListener {

    private final PipelineMetrics metrics;

    public BusLocationMetricsListener(PipelineMetrics metrics) {
        this.metrics = metrics;
    }

    @EventListener
    public void onBusLocationUpdated(BusLocationUpdatedEvent event) {
        metrics.busLocationReported(event.origin());
    }
}
```

- [ ] **Step 7: 알림 발송 계측용 Sender 구현**

`backend/src/main/java/src/backend/observability/sender/MetricsNotificationSender.java`:

```java
package src.backend.observability.sender;

import org.springframework.stereotype.Component;

import src.backend.notification.domain.NotificationLog;
import src.backend.notification.infrastructure.spec.NotificationSender;
import src.backend.observability.metrics.PipelineMetrics;

/**
 * 알림 발송을 계측한다. {@code NotificationCommandServiceImpl} 이 등록된
 * {@code NotificationSender} 를 전부 호출하므로, 구현체를 하나 더 얹는 것만으로 계측이 붙는다 —
 * 알림 서비스 코드는 수정하지 않는다.
 *
 * <p>중복 억제(dedupKey)로 저장을 건너뛴 경우에는 sender 가 호출되지 않는다. 따라서 이 카운터는
 * "시도 수"가 아니라 <b>실제 발송 수</b>이며, 알림 정지를 판별하는 값으로 쓸 수 있다.
 */
@Component
public class MetricsNotificationSender implements NotificationSender {

    private final PipelineMetrics metrics;

    public MetricsNotificationSender(PipelineMetrics metrics) {
        this.metrics = metrics;
    }

    @Override
    public void send(NotificationLog notification) {
        metrics.notificationSent(notification.getType());
    }
}
```

⚠ **`NotificationLog` 의 타입 접근자 이름을 실제 코드에서 확인해라.** Lombok `@Getter` 면 `getType()` 이지만
다르면 맞춰라. 확인 없이 추측해서 쓰지 마라.

- [ ] **Step 8: 도메인 파일 미변경 확인**

Run: `git status --short backend/src/main/java/src/backend | grep -v observability`
Expected: **출력 없음**

- [ ] **Step 9: 전체 테스트 + 커밋**

Run: `cd backend && ./gradlew test`

```bash
git add backend/src/main/java/src/backend/observability/ \
        backend/src/test/java/src/backend/observability/
git commit -m "feat(observability): Kafka 소비 실패·위치 수신·알림 발송 계측 — 도메인 수정 없이 (MON-9)"
```

---

## Task 4: STOMP 세션 수 계측

**Files:**
- Create: `backend/src/main/java/src/backend/observability/metrics/StompSessionMetrics.java`
- Test: `backend/src/test/java/src/backend/observability/StompSessionMetricsTest.java` (Create)

**Interfaces:**
- Consumes: Task 1 의 `/actuator/prometheus`
- Produces: 지표 `schoolbus_stomp_sessions` (Gauge)

⚠ **실측 근거:** 기존 `location/websocket/LocationSessionRegistry.java` 는 **끊긴 학생만** 보관하고 활성 세션 수를
갖고 있지 않다(`disconnectedAt` 맵). 그 클래스의 책임은 "연결끊김 유예 판정의 근거"이므로 넓히지 않는다.
Spring 이 발행하는 STOMP 연결·해제 이벤트를 구독해 **여기서 따로 센다.**

- [ ] **Step 1: 실패하는 테스트 작성**

`backend/src/test/java/src/backend/observability/StompSessionMetricsTest.java`:

```java
package src.backend.observability;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;

import org.junit.jupiter.api.Test;
import org.springframework.messaging.Message;
import org.springframework.web.socket.messaging.SessionConnectedEvent;
import org.springframework.web.socket.messaging.SessionDisconnectEvent;

import io.micrometer.core.instrument.simple.SimpleMeterRegistry;
import src.backend.observability.metrics.StompSessionMetrics;

class StompSessionMetricsTest {

    private final SimpleMeterRegistry registry = new SimpleMeterRegistry();
    private final StompSessionMetrics metrics = new StompSessionMetrics(registry);

    @Test
    void startsAtZero() {
        assertThat(sessions()).isEqualTo(0.0d);
    }

    @Test
    void connect_increments_disconnect_decrements() {
        metrics.onConnected(connectedEvent());
        metrics.onConnected(connectedEvent());
        assertThat(sessions()).isEqualTo(2.0d);

        metrics.onDisconnected(disconnectEvent());
        assertThat(sessions()).isEqualTo(1.0d);
    }

    /** 해제 이벤트가 중복으로 와도 음수로 내려가지 않는다 — 음수 게이지는 대시보드를 읽을 수 없게 만든다. */
    @Test
    void neverGoesNegative() {
        metrics.onDisconnected(disconnectEvent());
        metrics.onDisconnected(disconnectEvent());
        assertThat(sessions()).isEqualTo(0.0d);
    }

    private double sessions() {
        return registry.get("schoolbus.stomp.sessions").gauge().value();
    }

    @SuppressWarnings("unchecked")
    private SessionConnectedEvent connectedEvent() {
        return new SessionConnectedEvent(this, mock(Message.class));
    }

    @SuppressWarnings("unchecked")
    private SessionDisconnectEvent disconnectEvent() {
        return new SessionDisconnectEvent(this, mock(Message.class), "session-id", null);
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests '*StompSessionMetricsTest*'`
Expected: FAIL — 클래스 부재

- [ ] **Step 3: 구현**

`backend/src/main/java/src/backend/observability/metrics/StompSessionMetrics.java`:

```java
package src.backend.observability.metrics;

import java.util.concurrent.atomic.AtomicInteger;

import org.springframework.context.event.EventListener;
import org.springframework.stereotype.Component;
import org.springframework.web.socket.messaging.SessionConnectedEvent;
import org.springframework.web.socket.messaging.SessionDisconnectEvent;

import io.micrometer.core.instrument.Gauge;
import io.micrometer.core.instrument.MeterRegistry;

/**
 * 활성 STOMP 세션 수를 센다.
 *
 * <p>기존 {@code LocationSessionRegistry} 는 "끊긴 학생과 끊긴 시각"만 보관해 활성 수를 알 수 없다.
 * 그 클래스의 책임(연결끊김 유예 판정의 근거)을 넓히지 않고, 여기서 연결·해제 이벤트를 따로 센다.
 *
 * <p>세션 수는 실시간 push 파이프라인이 실제로 쓰이는지를 보여준다 — 0 이 지속되면
 * 관제·학부모 화면이 아무도 구독하지 않는 상태다.
 */
@Component
public class StompSessionMetrics {

    private final AtomicInteger activeSessions = new AtomicInteger();

    public StompSessionMetrics(MeterRegistry registry) {
        Gauge.builder("schoolbus.stomp.sessions", activeSessions, AtomicInteger::get)
                .description("활성 STOMP 세션 수")
                .register(registry);
    }

    @EventListener
    public void onConnected(SessionConnectedEvent event) {
        activeSessions.incrementAndGet();
    }

    /** 해제 이벤트가 중복으로 와도 0 아래로 내려가지 않게 막는다. */
    @EventListener
    public void onDisconnected(SessionDisconnectEvent event) {
        activeSessions.updateAndGet(current -> current > 0 ? current - 1 : 0);
    }
}
```

- [ ] **Step 4: 테스트 통과 + 도메인 미변경 확인**

Run: `cd backend && ./gradlew test --tests '*StompSessionMetricsTest*'`
Expected: PASS (3개)

Run: `git status --short backend/src/main/java/src/backend | grep -v observability`
Expected: 출력 없음

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/java/src/backend/observability/ \
        backend/src/test/java/src/backend/observability/
git commit -m "feat(observability): 활성 STOMP 세션 수 계측"
```

---

## Task 5: Prometheus + exporter 3종 기동

**Files:**
- Create: `infra/observability/prometheus/prometheus.yml`
- Modify: `docker-compose.yml` (services 블록 끝에 추가)

**Interfaces:**
- Consumes: Task 1~4 의 `/actuator/prometheus`
- Produces: `http://localhost:9090` 에서 조회 가능한 Prometheus. 스크레이프 job 이름 `backend` · `postgres` · `redis` · `node` — 대시보드 쿼리가 이 이름을 쓴다.

⚠ **백엔드 실행 방식에 따라 대상이 달라진다.** 이 저장소는 두 가지 로컬 실행 방식을 쓴다(`docker-compose.yml` 머리말).
컨테이너로 띄우면 `backend:8080`, IDE 에서 `./gradlew bootRun` 으로 띄우면 `host.docker.internal:8080` 이다.
아래 설정은 **컨테이너 기준**이며, bootRun 을 쓸 때는 주석의 한 줄로 바꾼다.

- [ ] **Step 1: 스크레이프 설정 작성**

`infra/observability/prometheus/prometheus.yml`:

```yaml
# 스크레이프 주기 15초 — 더 짧게 잡으면 시계열 증가폭 대비 관측 이득이 없다.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Spring Boot — 이 job 하나에서 HTTP 요청·JVM·HikariCP·Tomcat·Kafka 컨슈머·커스텀 지표가 모두 나온다.
  - job_name: backend
    metrics_path: /actuator/prometheus
    static_configs:
      # 백엔드를 컨테이너로 띄운 경우(docker compose up backend).
      - targets: ['backend:8080']
      # IDE 에서 ./gradlew bootRun 으로 띄우는 경우 위 줄을 아래로 바꾼다:
      # - targets: ['host.docker.internal:8080']

  - job_name: postgres
    static_configs:
      - targets: ['postgres-exporter:9187']

  - job_name: redis
    static_configs:
      - targets: ['redis-exporter:9121']

  - job_name: node
    static_configs:
      - targets: ['node-exporter:9100']

  # Prometheus 자기 자신 — 스크레이프가 밀리는지(scrape duration)를 보기 위해 둔다.
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
```

- [ ] **Step 2: compose 에 컨테이너 추가**

`docker-compose.yml` 의 `services:` 아래, `proxy` 서비스 뒤에 추가:

```yaml
  # ── 관측 스택 ──────────────────────────────────────────────────────────────
  # 설계 근거: docs/superpowers/specs/2026-08-21-관제-대시보드-design.md
  # exporter 들은 호스트에 포트를 열지 않는다 — Prometheus 가 컨테이너 네트워크에서만 긁는다.
  prometheus:
    image: prom/prometheus:v3.1.0
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      # 로컬은 보존 7일이면 충분하다(운영은 15일). 디스크와 메모리를 아낀다.
      - --storage.tsdb.retention.time=7d
    volumes:
      - ./infra/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      # 대시보드는 Grafana 로 보지만, 지표 이름·라벨을 직접 확인할 때 필요하다.
      - "9090:9090"
    depends_on:
      - backend

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.16.0
    environment:
      DATA_SOURCE_NAME: "postgresql://schoolbus:schoolbus@postgres:5432/schoolbus?sslmode=disable"
    expose:
      - "9187"
    depends_on:
      postgres:
        condition: service_healthy

  redis-exporter:
    image: oliver006/redis_exporter:v1.67.0
    environment:
      REDIS_ADDR: "redis://redis:6379"
    expose:
      - "9121"
    depends_on:
      - redis

  node-exporter:
    image: prom/node-exporter:v1.8.2
    # 호스트의 CPU·메모리·디스크를 읽기 위해 루트 파일시스템을 읽기 전용으로 마운트한다.
    command:
      - --path.rootfs=/host
    volumes:
      - /:/host:ro,rslave
    expose:
      - "9100"
```

- [ ] **Step 3: 기동**

Run: `docker compose up -d prometheus postgres-exporter redis-exporter node-exporter`
Expected: 4개 컨테이너 `Started`

- [ ] **Step 4: 스크레이프 대상 확인**

Run:
```bash
curl -s http://localhost:9090/api/v1/targets \
  | python3 -c 'import sys,json;[print(t["labels"]["job"], t["health"]) for t in json.load(sys.stdin)["data"]["activeTargets"]]'
```
Expected: `backend up` · `postgres up` · `redis up` · `node up` · `prometheus up`

`backend down` 이면 백엔드 실행 방식이 달라서다 — Step 1 의 주석대로 target 을 바꾸고 `docker compose restart prometheus`.

- [ ] **Step 5: 지표가 실제로 들어오는지 확인**

Run:
```bash
curl -s 'http://localhost:9090/api/v1/query?query=up' \
  | python3 -c 'import sys,json;[print(r["metric"]["job"], r["value"][1]) for r in json.load(sys.stdin)["data"]["result"]]'
```
Expected: 5개 job 모두 값 `1`

- [ ] **Step 6: 커밋**

```bash
git add infra/observability/prometheus/prometheus.yml docker-compose.yml
git commit -m "feat(observability): Prometheus + postgres/redis/node exporter 기동 (로컬)"
```

---

## Task 6: Grafana 기동 + 프로비저닝

**Files:**
- Create: `infra/observability/grafana/provisioning/datasources/prometheus.yml`
- Create: `infra/observability/grafana/provisioning/dashboards/dashboards.yml`
- Modify: `docker-compose.yml`

**Interfaces:**
- Consumes: Task 5 의 Prometheus(`http://prometheus:9090`)
- Produces: `http://localhost:3000` Grafana. datasource **uid `prometheus`** — 이후 모든 대시보드 JSON 이 이 uid 를 참조한다. 대시보드 파일 위치 `/var/lib/grafana/dashboards`.

- [ ] **Step 1: datasource 프로비저닝 작성**

`infra/observability/grafana/provisioning/datasources/prometheus.yml`:

```yaml
# 파일로 주입한다 — UI 에서 손으로 등록하면 컨테이너를 다시 만들 때 사라지고 재현이 안 된다.
apiVersion: 1

datasources:
  - name: Prometheus
    # uid 를 고정한다. 대시보드 JSON 이 이 값을 참조하므로 자동 생성 uid 를 쓰면 패널이 전부 깨진다.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
```

- [ ] **Step 2: 대시보드 폴더 프로비저닝 작성**

`infra/observability/grafana/provisioning/dashboards/dashboards.yml`:

```yaml
apiVersion: 1

providers:
  - name: school-bus
    type: file
    # UI 에서 수정해도 파일로 되돌아간다 — 대시보드의 단일 소스를 저장소에 둔다.
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
```

- [ ] **Step 3: compose 에 Grafana 추가**

`docker-compose.yml` 의 관측 스택 블록에 추가:

```yaml
  grafana:
    image: grafana/grafana:11.5.0
    environment:
      # ⚠️ 로컬 전용 값이다. 운영 노출(2단계) 시 반드시 바꾼다 — 기본 계정을 유지한 채
      #    서브도메인을 열면 그 시점에 관리 콘솔이 그대로 공개된다.
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      # 익명 열람 비활성. 로컬이라도 습관을 운영과 다르게 들이지 않는다.
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      # 외부 플러그인·뉴스 패널 등 바깥으로 나가는 호출을 끈다.
      GF_ANALYTICS_REPORTING_ENABLED: "false"
      GF_NEWS_NEWS_FEED_ENABLED: "false"
    volumes:
      - ./infra/observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infra/observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
```

- [ ] **Step 4: 대시보드 폴더 생성 (빈 폴더는 git 에 안 올라간다)**

Run: `mkdir -p infra/observability/grafana/dashboards && touch infra/observability/grafana/dashboards/.gitkeep`

- [ ] **Step 5: 기동 후 datasource 확인**

Run: `docker compose up -d grafana`
Run: `curl -s -u admin:admin http://localhost:3000/api/datasources | python3 -c 'import sys,json;[print(d["uid"], d["type"], d["url"]) for d in json.load(sys.stdin)]'`
Expected: `prometheus prometheus http://prometheus:9090`

- [ ] **Step 6: 커밋**

```bash
git add infra/observability/grafana docker-compose.yml
git commit -m "feat(observability): Grafana 기동 + datasource·대시보드 파일 프로비저닝"
```

---

## Task 7: 대시보드 1(API 개요) · 2(서버/JVM)

**Files:**
- Create: `infra/observability/grafana/dashboards/1-api.json`
- Create: `infra/observability/grafana/dashboards/2-server.json`

**Interfaces:**
- Consumes: Task 6 의 datasource uid `prometheus`, Task 1 의 `http_server_requests_seconds_*`
- Produces: Grafana 에 대시보드 2장. uid `school-bus-api` · `school-bus-server`

**패널 JSON 의 형태** — 아래 골격을 그대로 쓰고 `title`·`expr`·`gridPos` 만 바꿔 패널을 늘린다:

```json
{
  "uid": "school-bus-api",
  "title": "1. API 개요",
  "timezone": "browser",
  "refresh": "15s",
  "time": { "from": "now-1h", "to": "now" },
  "panels": [
    {
      "type": "timeseries",
      "title": "엔드포인트별 요청률 (req/s)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "targets": [
        {
          "refId": "A",
          "expr": "sum by (uri, method) (rate(http_server_requests_seconds_count{job=\"backend\"}[5m]))",
          "legendFormat": "{{method}} {{uri}}"
        }
      ]
    }
  ]
}
```

- [ ] **Step 1: 대시보드 1 작성 — 아래 6개 패널을 위 골격으로 만든다**

| 패널 | `expr` |
|---|---|
| 엔드포인트별 요청률 | `sum by (uri, method) (rate(http_server_requests_seconds_count{job="backend"}[5m]))` |
| 전체 에러율 (%) | `100 * sum(rate(http_server_requests_seconds_count{job="backend",status=~"5.."}[5m])) / sum(rate(http_server_requests_seconds_count{job="backend"}[5m]))` |
| 상태코드 분포 | `sum by (status) (rate(http_server_requests_seconds_count{job="backend"}[5m]))` |
| 응답시간 p95 (엔드포인트별) | `histogram_quantile(0.95, sum by (le, uri) (rate(http_server_requests_seconds_bucket{job="backend"}[5m])))` |
| 응답시간 p99 (엔드포인트별) | `histogram_quantile(0.99, sum by (le, uri) (rate(http_server_requests_seconds_bucket{job="backend"}[5m])))` |
| 느린 API 상위 10 (table) | `topk(10, histogram_quantile(0.95, sum by (le, uri) (rate(http_server_requests_seconds_bucket{job="backend"}[5m]))))` |

- [ ] **Step 2: 대시보드 2 작성 — spec §6.1 의 패널 전부**

**스레드**

| 패널 | `expr` |
|---|---|
| JVM 스레드 수 | `jvm_threads_live_threads{job="backend"}` · `jvm_threads_daemon_threads{job="backend"}` · `jvm_threads_peak_threads{job="backend"}` |
| 스레드 상태 분해 | `jvm_threads_states_threads{job="backend"}` (legend `{{state}}`) |
| Tomcat 스레드 풀 | `tomcat_threads_busy_threads{job="backend"}` · `tomcat_threads_current_threads{job="backend"}` · `tomcat_threads_config_max_threads{job="backend"}` |
| Tomcat 포화율 (%) | `100 * tomcat_threads_busy_threads{job="backend"} / tomcat_threads_config_max_threads{job="backend"}` |

**DB 커넥션 (HikariCP)**

| 패널 | `expr` |
|---|---|
| 커넥션 상태 | `hikaricp_connections_active{job="backend"}` · `hikaricp_connections_idle{job="backend"}` · `hikaricp_connections_pending{job="backend"}` |
| 획득 대기시간 p95 | `histogram_quantile(0.95, sum by (le) (rate(hikaricp_connections_acquire_seconds_bucket{job="backend"}[5m])))` |
| 커넥션 타임아웃 누적 | `increase(hikaricp_connections_timeout_total{job="backend"}[5m])` |
| 풀 사용률 (%) | `100 * hikaricp_connections_active{job="backend"} / hikaricp_connections_max{job="backend"}` |

**부하·자원**

| 패널 | `expr` |
|---|---|
| 시스템 load average | `node_load1` · `node_load5` · `node_load15` |
| CPU 사용률 | `process_cpu_usage{job="backend"}` · `system_cpu_usage{job="backend"}` |
| JVM heap | `jvm_memory_used_bytes{job="backend",area="heap"}` · `jvm_memory_max_bytes{job="backend",area="heap"}` |
| JVM non-heap | `jvm_memory_used_bytes{job="backend",area="nonheap"}` |
| 호스트 메모리 가용 | `node_memory_MemAvailable_bytes` |
| GC 일시정지 시간 | `rate(jvm_gc_pause_seconds_sum{job="backend"}[5m])` |
| 디스크 사용률 (%) | `100 - (node_filesystem_avail_bytes{mountpoint="/host"} / node_filesystem_size_bytes{mountpoint="/host"} * 100)` |
| 파일 디스크립터 | `process_files_open_files{job="backend"}` · `process_files_max_files{job="backend"}` |

- [ ] **Step 3: 대시보드 로드 확인**

Run: `docker compose restart grafana`
Run: `curl -s -u admin:admin http://localhost:3000/api/search?query= | python3 -c 'import sys,json;[print(d["uid"], d["title"]) for d in json.load(sys.stdin)]'`
Expected: `school-bus-api` · `school-bus-server` 2건

- [ ] **Step 4: 패널에 값이 실제로 들어오는지 확인**

Run:
```bash
curl -s 'http://localhost:9090/api/v1/query?query=tomcat_threads_busy_threads' \
  | python3 -c 'import sys,json;d=json.load(sys.stdin)["data"]["result"];print("결과", len(d), d[:1])'
```
Expected: 결과 1건 이상. **0건이면 해당 MeterBinder 가 미등록**이라 패널만 있고 지표가 없는 상태다.

- [ ] **Step 5: 커밋**

```bash
git add infra/observability/grafana/dashboards/1-api.json infra/observability/grafana/dashboards/2-server.json
git commit -m "feat(observability): 대시보드 API 개요·서버/JVM 2장 추가"
```

---

## Task 8: 대시보드 3(데이터 계층) · 4(파이프라인 생존)

**Files:**
- Create: `infra/observability/grafana/dashboards/3-data.json`
- Create: `infra/observability/grafana/dashboards/4-pipeline.json`

**Interfaces:**
- Consumes: Task 2~4 의 커스텀 지표, Task 5 의 exporter job
- Produces: 대시보드 uid `school-bus-data` · `school-bus-pipeline`

- [ ] **Step 1: 대시보드 3 작성 (spec §6.2)**

| 대상 | 패널 | `expr` |
|---|---|---|
| Postgres | 활성 커넥션 / 상한 | `sum(pg_stat_activity_count)` · `pg_settings_max_connections` |
| Postgres | 커밋·롤백률 | `rate(pg_stat_database_xact_commit[5m])` · `rate(pg_stat_database_xact_rollback[5m])` |
| Postgres | 락 대기 수 | `sum by (mode) (pg_locks_count)` |
| Postgres | 데드락 누적 | `increase(pg_stat_database_deadlocks[5m])` |
| Postgres | DB 크기 | `pg_database_size_bytes` |
| Redis | 사용 메모리 / 상한 | `redis_memory_used_bytes` · `redis_memory_max_bytes` |
| Redis | **축출 건수** | `increase(redis_evicted_keys_total[5m])` |
| Redis | 키 수 | `redis_db_keys` |
| Redis | 초당 명령 수 | `rate(redis_commands_processed_total[5m])` |
| Kafka | 컨슈머 소비 지연 | `kafka_consumer_fetch_manager_records_lag{job="backend"}` |

⚠ **Redis 축출 패널에 설명을 반드시 넣는다.** `maxmemory` 상한(`docker-compose.prod.yml:56` 에서 운영은 128MB)에 `allkeys-lru` 정책이라 메모리가 차면 **위치 좌표가 TTL 만료 전에 축출**된다. 그 경우 관제 지도에서 버스가 사라지는데 원인이 TTL 이 아니라 메모리 압박이다. 패널 `description` 에 이 문장을 넣어 두 원인을 혼동하지 않게 한다.

- [ ] **Step 2: 대시보드 4 작성 (spec §4.1)**

| 패널 | `expr` | 읽는 법 |
|---|---|---|
| 스케줄러 마지막 성공 후 경과 | `schoolbus_scheduler_last_success_age_seconds` (legend `{{scheduler}}`) | 각 스케줄러의 주기(3·10·15·30초)보다 크게 오래 늘면 정지 |
| 스케줄러 실패율 | `rate(schoolbus_scheduler_failures_total[5m])` | 0 이 아니면 반복 실패 |
| Kafka 소비 실패 | `increase(schoolbus_kafka_consume_failures_total[5m])` (legend `{{consumer}}`) | 컨슈머별 실패 |
| 버스 위치 수신률 | `rate(schoolbus_bus_location_reports_total[5m])` (legend `{{origin}}`) | 0 이면 보고 정지. `MOCK` 만 있으면 실단말 미연결 |
| 활성 STOMP 세션 | `schoolbus_stomp_sessions` | 0 지속이면 구독 화면 부재 |
| 알림 발송률 | `rate(schoolbus_notifications_sent_total[5m])` (legend `{{type}}`) | 0 이면 알림 파이프라인 정지 |

**스케줄러 경과 패널에 임계 색상을 넣는다** — 각 스케줄러 주기의 6배를 노랑, 20배를 빨강으로 둔다.
근거: 주기 한두 번 건너뛰는 것은 부하로도 발생하지만, 20배는 정지로 봐야 한다.
`sos-escalation`(30초 주기)이면 노랑 180초·빨강 600초다.

- [ ] **Step 3: 커스텀 지표가 실제로 나오는지 확인**

Run:
```bash
for m in schoolbus_scheduler_last_success_age_seconds schoolbus_stomp_sessions \
         schoolbus_bus_location_reports_total schoolbus_notifications_sent_total; do
  n=$(curl -s "http://localhost:9090/api/v1/query?query=$m" | python3 -c 'import sys,json;print(len(json.load(sys.stdin)["data"]["result"]))')
  echo "$m → $n 건"
done
```
Expected: `schoolbus_scheduler_last_success_age_seconds` 는 **4건**(스케줄러 4개). 나머지는 1건 이상.

`0 건`이면 해당 계측이 아직 호출되지 않은 것이다 — Counter 는 최초 증가 전까지 노출되지 않으므로,
버스 위치 보고·알림 발송을 한 번 일으킨 뒤 다시 확인한다.

- [ ] **Step 4: 커밋**

```bash
git add infra/observability/grafana/dashboards/3-data.json infra/observability/grafana/dashboards/4-pipeline.json
git commit -m "feat(observability): 대시보드 데이터 계층·파이프라인 생존 2장 추가"
```

---

## Task 9: 전체 검증 + 문서 갱신

**Files:**
- Modify: `backend/docs/plans/2026-08-21-모니터링-구현계획.md` (§4 진행 추적)
- Modify: `docs/ARCHITECTURE.md` (인프라 표에 관측 스택 추가)
- Test: 없음 — 이 Task 는 **실행 결과 재현**이 산출물이다

**Interfaces:**
- Consumes: Task 1~8 전부
- Produces: spec §11 의 8개 검증 항목 재현 기록

**완료 판정 기준:** 코드 작성이 아니라 **아래 8개를 실제로 재현한 출력**이다. 재현하지 못한 항목은 "미검증"으로 표기하고 완료로 적지 않는다.

- [ ] **Step 1: 지표 노출 경로 확인 (spec §11-1)**

```bash
docker compose exec -T prometheus wget -qO- http://backend:8080/actuator/prometheus | head -3
curl -s -o /dev/null -w "로컬 프록시 경유 /actuator → %{http_code}\n" http://localhost/actuator/prometheus
```
Expected: 내부에서는 지표 텍스트가 나오고, 프록시 경유는 지표가 노출되지 않는다.

⚠ **로컬 프록시(`infra/proxy/nginx.conf`)에 `/actuator` 차단 규칙이 없으면 200 이 나온다.** 그 경우
**운영 설정(`nginx.prod.conf:149`)에만 차단이 있는 상태**이므로, 결과를 그대로 기록하고 로컬 차단 추가 여부는
사용자에게 확인한다. **임의로 `nginx.prod.conf` 를 고치지 않는다**(Global Constraints).

- [ ] **Step 2: 스크레이프 대상 확인 (spec §11-2)**

```bash
curl -s http://localhost:9090/api/v1/targets \
  | python3 -c 'import sys,json;[print(t["labels"]["job"], t["health"], t.get("lastError","")) for t in json.load(sys.stdin)["data"]["activeTargets"]]'
```
Expected: 5개 job 전부 `up`

- [ ] **Step 3: URI cardinality 음성 대조 (spec §11-3, R1)**

```bash
TOK=$(curl -s -X POST localhost:8080/api/auth/login -H 'Content-Type: application/json' \
  -d '{"email":"admin@school.com","password":"password"}' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["data"]["accessToken"])')
for id in 1 2 3; do curl -s -o /dev/null "localhost:8080/api/buses/$id" -H "Authorization: Bearer $TOK"; done

curl -s 'http://localhost:9090/api/v1/query?query=count(count%20by%20(uri)(http_server_requests_seconds_count))' \
  | python3 -c 'import sys,json;print("고유 uri 시계열 수:", json.load(sys.stdin)["data"]["result"][0]["value"][1])'
curl -s 'http://localhost:9090/api/v1/label/uri/values' \
  | python3 -c 'import sys,json;print([u for u in json.load(sys.stdin)["data"] if "buses" in u])'
```
Expected: `['/api/buses/{id}', ...]` 형태의 **패턴**만 나오고 `/api/buses/1` 같은 실제 id 는 나오지 않는다.
id 가 그대로 보이면 **즉시 중단하고 보고한다** — 시계열이 무한히 늘어난다.

- [ ] **Step 4: 스케줄러 정지 관측 (spec §11-4, MON-8 완료 조건)**

인위적으로 실패를 만들어 게이지가 증가하는지 본다. Redis 를 멈추면 `ConnectionLossScheduler` 가 실패한다.

```bash
BEFORE=$(curl -s 'http://localhost:9090/api/v1/query?query=schoolbus_scheduler_last_success_age_seconds{scheduler="connection-loss"}' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["data"]["result"][0]["value"][1])')
docker compose stop redis
sleep 60
AFTER=$(curl -s 'http://localhost:9090/api/v1/query?query=schoolbus_scheduler_last_success_age_seconds{scheduler="connection-loss"}' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["data"]["result"][0]["value"][1])')
echo "경과: $BEFORE → $AFTER (증가해야 한다)"
curl -s 'http://localhost:9090/api/v1/query?query=schoolbus_scheduler_failures_total' \
  | python3 -c 'import sys,json;[print(r["metric"]["scheduler"], r["value"][1]) for r in json.load(sys.stdin)["data"]["result"]]'
docker compose start redis
```
Expected: 경과 값이 증가하고 `connection-loss` 실패 카운터가 0 보다 크다. **복구 후 경과가 다시 0 근처로 내려간다.**

⚠ **`docker compose stop` 을 쓴다. `down` 을 쓰지 않는다** — 컨테이너를 제거하면 다른 서비스까지 재구성된다.

- [ ] **Step 5: 위치 보고 정지 관측 (spec §11-5)**

```bash
curl -s 'http://localhost:9090/api/v1/query?query=rate(schoolbus_bus_location_reports_total[1m])' \
  | python3 -c 'import sys,json;[print(r["metric"]["origin"], r["value"][1]) for r in json.load(sys.stdin)["data"]["result"]]'
```
Expected: 서버 Mock 이 켜져 있으면(`app.location.bus-mock.enabled=true` 기본값) `MOCK` 이 0 보다 크다.
`--app.location.bus-mock.enabled=false` 로 백엔드를 재기동하면 1분 뒤 0 으로 내려간다.

- [ ] **Step 6: 프로비저닝 복원 확인 (spec §11-6)**

```bash
docker compose rm -sf grafana && docker compose up -d grafana && sleep 10
curl -s -u admin:admin 'http://localhost:3000/api/search?query=' \
  | python3 -c 'import sys,json;[print(d["uid"], d["title"]) for d in json.load(sys.stdin)]'
```
Expected: 대시보드 4장이 **손으로 다시 만들지 않고** 그대로 복원된다.

- [ ] **Step 7: 메모리 실사용 측정 (spec §11-7, R3)**

```bash
docker stats --no-stream --format '{{.Name}}\t{{.MemUsage}}' \
  | grep -E 'prometheus|grafana|exporter'
```
Expected 대조: spec §3.1 예산(prometheus ~300MB · grafana ~200MB · exporter 3종 ~55MB, 합계 ~550MB).
**예산을 크게 넘으면 수치를 그대로 기록하고 보고한다** — 보존 기간·스크레이프 주기 조정이 먼저다.

- [ ] **Step 8: MeterBinder 등록 확인 (spec §11-8)**

```bash
for m in tomcat_threads_busy_threads hikaricp_connections_active jvm_threads_states_threads \
         jvm_gc_pause_seconds_sum process_files_open_files; do
  n=$(curl -s "http://localhost:9090/api/v1/query?query=$m" \
    | python3 -c 'import sys,json;print(len(json.load(sys.stdin)["data"]["result"]))')
  echo "$m → $n 건"
done
```
Expected: 전부 1건 이상. **0건이면 그 패널은 항상 빈 값**이므로 해당 지표를 대시보드에서 빼거나 등록 원인을 찾는다.

- [ ] **Step 8b: 무침투 원칙 최종 확인 (전 브랜치 기준)**

관측 작업이 도메인 코드를 한 줄도 바꾸지 않았는지 브랜치 전체로 확인한다.

```bash
git diff --name-only <이 계획 시작 커밋>..HEAD -- backend/src/main/java/src/backend \
  | grep -v '^backend/src/main/java/src/backend/observability/' || echo "도메인 수정 없음"
```
Expected: `도메인 수정 없음`. 파일이 나오면 그 파일과 이유를 보고서에 적는다 — 무침투가 깨진 것이므로
설계 §5.3 을 사실에 맞게 고치거나 되돌린다.

- [ ] **Step 9: 계획서 진행 추적 갱신**

`backend/docs/plans/2026-08-21-모니터링-구현계획.md` §4 에서 아래를 `✅ 완료` + 커밋 해시로 바꾼다:
`MON-8`(Task 2) · `MON-9`(Task 3) · `MON-10`(Task 1) · `MON-11`(Task 1·5·6).

**`MON-13`(CloudWatch 알람)은 완료로 표기하지 않는다** — 이 대시보드는 조회 수단이고 통지 수단이 아니다(spec §12).
**`MON-12`(구조화 로그)도 범위 밖이다.**

- [ ] **Step 10: 아키텍처 문서 갱신**

`docs/ARCHITECTURE.md` 의 인프라 표(32행 부근 `| 캐시 | Redis 7 | ... |` 이 있는 표)에 행을 추가한다:

```markdown
| 관측 | Prometheus 3 + Grafana 11 | 지표 수집·대시보드 4장. `/actuator/prometheus` 를 내부망에서만 스크레이프 | `9090:9090` · `3000:3000` |
```

⚠ **`docs/DEPLOYMENT.md:591` 의 "알람·APM 부재 · 장애 인지는 수동"은 고치지 않는다.** 알람은 여전히 부재이며,
이 작업으로 바뀐 것은 **조회 수단의 유무**뿐이다. 사실과 다르게 완화하면 다음 세션이 알람이 있다고 오인한다.

- [ ] **Step 11: 커밋**

```bash
git add backend/docs/plans/2026-08-21-모니터링-구현계획.md docs/ARCHITECTURE.md
git commit -m "docs: 관제 대시보드 구축 반영 — MON-8·9·10·11 완료 (알람 MON-13 은 미착수)"
```

---

## Self-Review

**1. 스펙 커버리지**

| spec 절 | 담당 Task |
|---|---|
| §3.1 컨테이너 5개 | Task 5(4개) · Task 6(grafana) |
| §3.2 스크레이프 경로·차단 유지 | Task 1(노출) · Task 5(스크레이프) · Task 9-1(검증) |
| §3.3 설정 코드화 | Task 6 · Task 9-6(재생성 복원 검증) |
| §4.1 Grafana 지표 6종 | Task 2(스케줄러 2) · Task 3(Kafka·위치·알림 3) · Task 4(STOMP 1) |
| §4.2 식별자 태그 미유입 | Task 3-Step 1 의 음성 대조 테스트 |
| §5.1 자동 지표 | Task 1(의존성·히스토그램) |
| §5.2 커스텀 계측 | Task 2·3·4 |
| §5.3 무침투(도메인 0줄) | Task 2-Step 9 · Task 3-Step 8 · Task 4-Step 4 · Task 9-Step 8b |
| §6 대시보드 4장 | Task 7(1·2) · Task 8(3·4) |
| §6.1 서버 상태 패널 | Task 7-Step 2 |
| §6.2 데이터 계층 패널 | Task 8-Step 1 |
| §7 자원 예산 | Task 5(보존 7일) · Task 9-7(실측 대조) |
| §9 보안 | Task 5(exporter 미개방) · Task 6(기본 계정 경고·익명 비활성) |
| §10 R1 cardinality | Task 1-Step 1 테스트 · Task 9-3 |
| §10 R2 대시보드 동반 정지 | Task 9-9 에서 MON-13 미완료 유지로 반영 |
| §10 R3 메모리 | Task 9-7 |
| §11 검증 8항목 | Task 9 |
| §12 범위 밖 | Task 9-9·9-10 에서 명시적으로 미표기 |

**누락 없음.**

**2. 자리표시자 점검** — "TBD"·"적절히 처리"·"위와 유사" 형태 없음. 모든 코드 단계에 실제 코드 포함.

**3. 타입 일관성**

| 이름 | 정의 | 사용 |
|---|---|---|
| `SchedulerHealthMetrics#recordSuccess(String)` | Task 2-Step 3 | Task 2-Step 5 |
| 스케줄러 태그 4종 | Task 2-Step 3 상수 | Task 8-Step 2 쿼리 legend |
| `PipelineMetrics#busLocationReported(LocationOrigin)` | Task 3-Step 3 | Task 3-Step 5 |
| 지표명 `schoolbus.*` (Micrometer 점 표기) | Task 2·3·4 | Task 8 쿼리는 `schoolbus_*` 밑줄 표기 — **Prometheus 변환 규칙에 따른 의도된 차이** |
| datasource uid `prometheus` | Task 6-Step 1 | Task 7 골격 JSON |
| job 이름 `backend`·`postgres`·`redis`·`node` | Task 5-Step 1 | Task 7·8 쿼리 |

**Counter 접미사 주의:** Micrometer Counter 는 Prometheus 노출 시 `_total` 이 붙는다.
`schoolbus.scheduler.failures` → `schoolbus_scheduler_failures_total`. Task 8 쿼리는 이 규칙을 따랐다.

---

## 실행 방식

이 계획은 **Task 1 → 9 순서 의존**이다. Task 5 이후는 Task 1~4 의 지표가 없으면 대시보드가 빈 값이 된다.

| Task | 선행 | 병렬 가능 |
|---|---|---|
| 1 | — | — |
| 2·3·4 | 1 | **서로 병렬 가능**(파일 집합이 분리됨. 단 2·3 이 같은 스케줄러/컨슈머 파일을 건드리지 않는지 확인) |
| 5 | 1 | — |
| 6 | 5 | — |
| 7·8 | 2·3·4·6 | 서로 병렬 가능 |
| 9 | 전부 | — |
