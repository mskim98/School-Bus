# MVP 확장 — SoT 문서 갱신 (DOC-1 ~ DOC-5)

> **에이전트에게:** 공용 계약·결정·**커밋 규약(§1.5)** 은 [`2026-08-02-mvp-확장-OVERVIEW.md`](./2026-08-02-mvp-확장-OVERVIEW.md) 에만 있다. **먼저 읽고** 온다.
> 이 문서는 **구현이 전부 끝난 뒤**(P6) 실행한다. 구현 전에 손대면 안 되는 이유는 §0 에 있다.

- 작성일: 2026-08-02 (요구 **A1·A2** 반영해 개정) · 선행: `BE-1`~`BE-14` · `FE-0`~`FE-12` **전부 완료**
- 대상은 저장소 루트 `docs/` 4종 + `CLAUDE.md` + `docs/README.md`

> 이 문서는 **두 갈래의 결과**를 한꺼번에 반영한다. 헷갈리지 않게 태스크마다 어느 갈래인지 표시했다.
> - **운행 축** — 선탑자 승격 · 학부모 앱 · 배차 시뮬레이션 · 관제 상세 (`BE-1`~`BE-11`, `FE-1`~`FE-10`)
> - **인적 축** — 계정 CRUD · 수동 매칭 (`BE-12`~`BE-14`, `FE-11`~`FE-12`) ← **2026-08-02 추가분**

---

## 0. 왜 구현 후인가 (먼저 읽을 것)

`docs/` 4종은 **기획서가 아니라 실측 기록**이다. 특히 `PRODUCT_SPEC.md` §11 은 스스로를 이렇게 규정한다:

> *"이 장이 '지금 실제로 되는 것'의 유일한 근거다. 여기 없는 기능은 구현되지 않은 것이다."*

그래서 **구현 전에 ✅ 를 미리 적으면 그 자체가 허위**가 된다. 이 저장소는 이미 그 사고를 겪었다 —
`projectInfo.md` §3.1 이 "프론트엔드 MVP 데모에서 실제 화면으로 구현됨"이라고 적어 둔 화면들이 하나도 없어서,
`PRODUCT_SPEC.md` §0 이 그것을 **"흔한 오해"로 따로 경고**하고 있다. 같은 일을 반복하지 않는다.

⚠️ **`docs/plans/`(계획)와 `docs/`(사양)는 성격이 다르다.** 계획서는 구현이 끝나면 역할이 끝나지만
SoT 4종은 계속 유지된다. **계획서 내용을 사양으로 복사하지 마라** — 사양에는 "만들 것"이 아니라 "있는 것"만 적는다.

**공통 원칙**
- 표기는 `✅ 구현 / 🟡 부분 / ⬜ 미구현 / ➖ 범위 밖` 4개만 쓴다(`docs/README.md` §2)
- 모든 사실에 **파일 경로나 엔드포인트**를 붙인다. 근거 없는 서술을 두지 않는다
- 값(개수·수치)은 **추정하지 말고 실제로 세서** 적는다 — §5 에 세는 명령이 있다
- ⚠️ **이 단계의 산출물은 Markdown 뿐이다**(2026-08-02 확정). `DOC-1`~`DOC-5` 는 `.md` 만 고친다.
  HTML 렌더(`backend/docs/*.html` 등)는 **모든 개발이 끝난 뒤 별도 단계**로 한 번에 변환한다 —
  구현이 계속 바뀌는 중에 두 매체를 동기화하면 **둘 다 낡는다.** 어느 `.html` 도 열지 않는다.

---

## DOC-1: `docs/PRODUCT_SPEC.md` (가장 무겁다)

**목적:** 6계층·새 엔티티·바뀐 정책·새 구현 상태를 실측대로 반영한다.
**Files:** Modify `docs/PRODUCT_SPEC.md`

- [ ] **Step 1: §2 사용자 계층 — 5 → 6계층**
  - 계층 표에 `ATTENDANT 선탑자` 행 추가(조회 범위 = 담당 버스 명단·승하차 기록 / MVP 화면 = 2화면 / 백엔드 = ✅)
  - ⚠️ **본문의 이 문장을 반드시 지운다:** *"동승보호자는 MVP에서 별도 역할로 분리하지 않고 기사 앱의 승하차 확인 흐름에 흡수했다(추후 역할 승격 가능)"* → **승격됐다.** 남겨두면 정반대 사실이 SoT 에 남는다
  - "정의된 역할 5개" 를 언급하는 다른 문장도 함께 찾아 고친다

- [ ] **Step 2: §3 데이터 모델 — 엔티티 16 → 17개**
  - `LocationChangeRequest`(schedule 모듈) 추가. §3.1 "운영 데이터" 목록에 넣는다
  - `Student` 행의 🟡 사유에서 **"사진 필드 없음"을 뺀다**(생겼다). `pickup_*`·`phone` 도 반영
  - `Bus` 행에 선탑자(`attendant_id`) 보유 사실 추가
  - §3.2 설계 결정에 **D-K**(등원 좌표를 `Student` 자체 컬럼으로, 공유 `Stop` 을 고치지 않는 이유)를 한 줄로 넣는다
  - ⚠️ 마이그레이션 설명이 "V1 은 수정하지 않는다" 로 돼 있다 → **개발 단계 한정 수정 허용**으로 고친다(D-D). `DOC-5` 와 같은 사실이다

- [ ] **Step 3: §3.3 enum 표 갱신**
  - `Role` 에 `ATTENDANT` 추가
  - `NotificationType` 10종 → **11종**(`LOCATION_CHANGE_RESULT`)
  - 신규 행 2개: `LocationChangeDecision`(APPLIED/REPLANNED/REJECTED/BLOCKED) · `OverrideAction`(ADD/REMOVE/MOVE)

- [ ] **Step 4: §4.1 승인 정책 개정 ← 가장 조심할 곳**
  - 현재 문장: *"기사·학부모가 직접 합의한 변경도 학원 승인을 거쳐야 효력이 생긴다"*
  - → **위치 변경에 한해** 임계(소요시간 +300초 **AND** 거리 +1000m) 이내면 **무승인 자동 적용**, 초과면 **자동 거부**로 고친다
  - ⚠️ **"승인이 사라졌다"로 뭉뚱그리지 마라.** 시간 변경(`ScheduleChangeRequest`)은 여전히 관리자 승인이 필요하다. 둘을 구분해 적는다
  - 자동 적용도 **요청 행이 남는다**는 사실을 함께 적는다(감사 로그가 없는 저장소라 이게 유일한 흔적이다)

- [ ] **Step 5: §5 승하차 판정 주체 변경**
  - §5.2 *"실제 구현된 경로는 기사 수동 기록 하나뿐"* → **선탑자** 수동 기록으로
  - §5.3 운행 세션 설명에 **분업**을 명시: 세션 시작·종료는 기사, 승하차 기록은 선탑자. 차내 잔류 방지가 두 역할의 협업 지점이 된다(선탑자가 하차를 다 찍어야 기사가 종료할 수 있다)
  - §5.2 "사진 육안 확인은 `Student` 에 사진 필드가 없어 불가능하다" → 필드가 생겼으므로 **현재 상태로 다시 쓴다**

- [ ] **Step 6: §7 실시간 — 버스 위치 push**
  - §7.1 표의 `서버 → 관리자 (관제)` 행: **3초 REST 폴링 🟡 → STOMP push ✅** (`BE-8` 결과)
  - 학부모 행 신설: `/user/queue/bus-location`
  - *"버스 위치는 저장은 되지만 이벤트를 발행하지 않아 실시간 push로 이어지지 않는다"* → **발행한다**로 고친다

- [ ] **Step 7: §11 구현 매트릭스 전면 갱신 ← 실측 필수**
  - §11.1 수치표를 §5 의 명령으로 **직접 세서** 채운다(엔드포인트·프론트 호출 수·라우트 수·화면 있는 역할)
  - §11.2 각 표에 신규 행 추가: 선탑자 승하차 기록 · 배차 시뮬레이션·채택 · 학부모 자녀 상태/버스 위치/위치변경 · 관리자 버스 상세 종합
  - 기존 행 중 상태가 바뀐 것: 승하차 기록 정정(⬜→✅) · 버스 위치 실시간(🟡→✅) · 학부모 앱(⬜→🟡 또는 ✅) · 회원가입 권한(보안 수정 반영)
  - ⚠️ "역할별 앱" 표의 **학생 앱은 여전히 ⬜ 0화면**이다. 학부모와 묶어 처리하지 마라(D-F)

- [ ] **Step 8: §12 갭 목록 재계산**
  - 해소된 것을 **지운다**(O2 SOS 는 여전히 남는다 — 이번 범위 아님). 최소: `F13`(정정 UI) · `F9`(버스 위치 push) · 12.4 의 "회원가입 권한 상승"·"참조 무결성 미검증(기사 배정)"
  - `O1`(학생·학부모 화면 0개)은 **학생만 남기고** 문구를 고친다
  - 남은 건수(현재 "총 28건 — 운영불가 5 · 기능누락 15 · 사소 8")를 **다시 세서** 고친다
  - §12.5 우선순위 표에서 처리된 항목을 빼고 순위를 다시 매긴다

### 【인적 축】 A1·A2 추가분 — `DOC-1` Step 9~13

> 줄번호는 **2026-08-02 실측**이다. 앞 Step 들이 줄 수를 바꾸므로 **번호가 아니라 인용문으로 찾는다.**

- [ ] **Step 9: §11.2 `#### 운영 관리` 표 4행 갱신 + 3행 신설** (현재 L359~372)

  | 현재 행(인용) | 어떻게 |
  |---|---|
  | `구성원(회원) 목록·등록 \| ✅ \| 🟡 \| ⬜ \| 생성·조회만 있고 **수정·삭제 API가 없다**(퇴사·기사 교체 불가). 화면 없음` | 제목을 **`구성원 CRUD`** 로, 백엔드 `🟡→✅`, 프론트 `⬜→✅`. 근거를 **"삭제가 아니라 학원 멤버십 해제"**(D-O)로 |
  | `학생 등록·재배정·하차지 설정 \| ✅ \| 🟡 \| ⬜ \| … 보호자·계정 역할 미검증. **화면 없음**` | 프론트 `⬜→✅`. **"계정 역할 미검증"을 지운다** — D-N 으로 학생 계정을 만들지 않아 전제가 사라졌다 |
  | `보호자 연결 \| ✅ \| 🟡 \| ⬜ \| 중복 연결 방지·역할 검증 없음. 화면 없음` | 제목을 **`보호자 연결·해제`** 로. 중복 방지·역할 검증이 `BE-14` 로 해소 |
  | `버스 등록·배차 변경 \| ✅ \| 🟡 \| ⬜ \| … 기사 역할·소속 미검증, 배정 해제 불가` | 역할·소속 미검증은 **`BE-7`** 이, 중복 배정은 **`BE-14`** 가 해소. ⚠ **"배정 해제 불가"는 그대로 남는다** — 이번 범위가 아니다 |

  신설 3행: **학생 비활성화(퇴원)** · **구성원 비밀번호 재설정** · **학부모 기준 자녀 역방향 조회**.
  ⚠ `학원(테넌트) 등록·depot 좌표 설정` 행은 **손대지 않는다.** A1 범위는 학생·학부모·기사·선탑자이고 테넌트는 아니다.

- [ ] **Step 10: §12 갭 3건 해소 + 총계 재계산** ← **가장 조심할 곳**

  | 갭 | 인용 | 처리 |
  |---|---|---|
  | **O4** | *"그 화면이 존재하지 않는다. 프론트의 HTTP 클라이언트에는 `put`·`delete` 메서드조차 없고 … 데이터는 오직 **DB 시드나 Swagger로만** 들어간다"* | **해소.** 세 문장 전부 거짓이 된다(`FE-11` 이 `ApiClient.delete` 를 만든다) |
  | **F15** | *"계정을 만들 경로가 앱 안에 전혀 없다 … 그 관리자용 생성 화면도 없다"* | **해소** |
  | **F12** | *"마스터 데이터의 수정·삭제 API가 없다 — 구성원은 생성·조회만 있어 퇴사 처리·기사 교체가 불가능하고, 버스·학생의 배정은 null로 보내도 '변경 없음'으로 해석돼 **배정 해제 수단이 없다**"* | ⚠️ **절반만 해소된다.** 앞 절반(구성원 수정·해제)은 끝났지만 **뒤 절반(배정 해제 수단 없음)은 그대로다** — `PATCH /assignment` 의 `null = 변경 없음` 해석은 이번에 건드리지 않았다. **F12 를 통째로 지우지 말고 뒤 절반만 남긴다** |

  - §12.4 불릿 *"참조 무결성 미검증: 버스에 담당 기사를 지정할 때 역할·소속을 확인하지 않는다 … 학생 등록 시 연결 계정은 존재 확인조차 하지 않는다"* → **앞문장은 `BE-7` 이, 뒷문장은 D-N 이 없앤다**(연결할 계정 자체를 안 만든다). 불릿을 지운다.
  - §12.2 에 **신규 갭 없음**을 확인한다 — 보호자 연결 해제·역방향 조회는 **원래 갭으로 등재돼 있지 않았다**(실측). 없던 갭을 해소했다고 적지 않는다.
  - 총계 문장 *"총 **28건** — 운영불가 5 · 기능누락 15 · 사소 8"* 과 소제목 `12.1 운영불가 (5건)` · `12.2 기능누락 (15건)` 을 **다시 세서** 고친다.
  - §12.5 우선순위 **1행**(*"O4/F15 관리자 데이터 관리 화면 … 다른 모든 기능의 전제"*)을 빼고 **순위를 1부터 다시 매긴다.**

  > ⚠️ **기존 상호참조 결함 1건**(A1·A2 와 무관): §3.3 근처가 `(→ 12장 F-3)` 으로 가리키는 대상이 실제 `F3`(학원 1번 폴백 하드코딩)과 다르고 표기도 `F-3`/`F3` 로 어긋난다. **번호를 재배열하면 더 꼬인다** — 재배열 전에 이 참조부터 고치거나, 최소한 건드리지 않는다.

- [ ] **Step 11: 관리자 화면 4 → 5** — 같은 수치가 **§2 역할표 2행 · §11.1 수치표 · §11.2 역할별 앱 표** 에 흩어져 있다. `4화면` 을 전수 검색해 **한 번에** 고친다. 라우트 총계(현재 9개)와 프론트 호출 엔드포인트(현재 22개)도 §5 명령으로 다시 센다.

- [ ] **Step 12: §3 데이터 모델에 인적 축 3개를 넣는다**
  - §3 매핑표 `학생정보` 행에 **`active`(활성 여부)** 추가, 🟡 사유에서 사진 관련 문구 정리
  - §3 매핑표 `차량정보` 행에 **`선탑자(attendant)`** 추가 — ⚠️ **`attendant` 는 이 문서에 지금 0회 등장한다**(실측). D-P 가 "유일한 매개"로 지목한 필드가 사양서에 없는 상태다
  - §3.2 설계 결정에 **D-O**(물리 삭제 금지)와 **D-P**(매칭 전용 테이블 없음)를 불릿으로. §3.2 는 이미 *"정정은 append-only"* 같은 **데이터 보존 원칙**을 모아둔 절이라 자리가 맞는다

- [ ] **Step 13: 전제가 바뀐 서술 4곳을 고친다**
  - §2 *"동승보호자는 MVP에서 별도 역할로 분리하지 않고…"* → Step 1 이 이미 지운다. **중복 작업하지 않는다**
  - §2 계층표 `STUDENT` 행 — **"신규 학생 계정은 만들지 않는다"**(D-N)를 단서로 단다. 시드 계정은 남아 있다
  - §2 흔한 오해 *"두 역할은 로그인은 성공하지만 '준비 중' 배너만 본다"* → 학부모는 화면이 생겼고(`FE-7`~`FE-10`), **학생만 해당**한다
  - §11 흔한 오해 1 *"버스/학생/회원 관리는 백엔드가 다 구현돼 있고 프론트 화면만 없다 … 남은 일은 'API 구현'이 아니라 '화면 구현'"* → ⚠️ **이 결론이 부분적으로 무너진다.** A1 은 백엔드 API 도 8개 새로 만들었다. 문장을 완화한다
  - §8(개인정보) *"위 항목 전부 ⬜ 미구현"* → *"퇴사·노선변경 시 권한 즉시 회수"* 가 D-O 멤버십 해제로 **부분 구현**된다. `⬜ → 🟡` 인지 실측으로 판단한다

**검증:** `docs-drift-auditor` 에이전트에 `docs/PRODUCT_SPEC.md` 와 `backend/src`·`frontend/lib` 를 대조시킨다. 지적이 0건이 될 때까지 고친다.

---

## DOC-2: `docs/USER_FLOWS.md`

**목적:** 새 역할 2개의 여정을 넣고, 기사 여정에서 사라진 단계를 뺀다.
**Files:** Modify `docs/USER_FLOWS.md`

- [ ] **Step 1: 역할별 진입 지도에 선탑자·학부모 추가** — 각 역할의 첫 화면과 라우트를 적는다
- [ ] **Step 2: 선탑자 플로우 신규** — 로그인 → 담당 버스 확인 → (기사가 시작한) 세션 명단 → 정차별 승차/하차/인계 기록 → 오기록 정정. **"행동 → 화면 → API → 결과"** 4열 형식을 지킨다
- [ ] **Step 3: 학부모 플로우 신규 2개** — ① 자녀 등하원 상태 확인 + 버스 실시간 위치 ② 등하원 위치 변경 신청 → **4갈래 결과**(APPLIED/REPLANNED/REJECTED/BLOCKED). ⚠️ 거부는 **실패가 아니라 정상 결과**다. 에러 흐름으로 그리지 마라
- [ ] **Step 4: 기사 플로우 수정** — 승하차 기록 단계를 **뺀다**. 종료 시 "선탑자가 하차를 마쳐야 종료된다"는 대기 지점을 넣는다
- [ ] **Step 5: A부(구현됨) / B부(설계됨·미구현) 재분류** — 이번에 구현된 것을 B부에서 A부로 옮긴다

### 【인적 축】 A1·A2 추가분 — `DOC-2` Step 6~8

> ⚠️ **새 플로우를 처음부터 쓰지 마라. 이미 설계돼 있다.** `B6. 기초 데이터 등록 (버스 · 학생 · 회원 · 학원) ⬜` 이
> **10단계 표로 존재**하고, 심지어 화면 경로까지 지정돼 있다 — `/admin/members`(3번), `/admin/students`(7번),
> `/admin/students/{id}`(8·9·10번). `FE-11` 이 만드는 경로와 **거의 그대로 맞는다.**
> 할 일은 신규 작성이 아니라 **B6 를 채우고 A부로 승격**하는 것이다.

- [ ] **Step 6: `B6` 표에 8행을 더한다.** 기존 10행의 6열 형식(`# / 사용자 / 행동 / (있어야 할) 화면 / 호출 API / 결과`)을 그대로 쓴다.

  | 행동 | 화면 | API | 결과 |
  |---|---|---|---|
  | 구성원 상세 열기 | `/admin/members` | `GET /api/members/{id}` | 배정 버스·담당 학생이 함께 온다(삭제 가능 여부 판단 근거) |
  | 구성원 정보 수정 | `/admin/members` | `PATCH /api/members/{id}` | ⚠ **그 사람이 속한 모든 학원에서 이름·전화가 함께 바뀐다**(`app_user` 가 한 벌이다) |
  | 비밀번호 재설정 | `/admin/members` | `PATCH /api/members/{id}/password` | ⚠ **관리자 계정은 대상이 아니다.** 기존 세션도 즉시 만료되지 않는다 |
  | 구성원 내리기 | `/admin/members` | `DELETE /api/members/{id}` | **학원 멤버십만 해제.** 계정 행은 남는다(D-O). 참조 중이면 409 |
  | 학생 정보 수정 | `/admin/members`(학생 탭) | `PATCH /api/students/{id}` | 이름·전화·사진. 배차·하차지는 기존 전용 API |
  | 학생 퇴원 처리 | `/admin/members`(학생 탭) | `DELETE /api/students/{id}` | `active=false`. **명단·배차에서 빠지되 과거 승하차 기록은 남는다** |
  | 보호자 연결 해제 | `/admin/members`(학생 상세) | `DELETE /api/students/{id}/guardians/{guardianUserId}` | `student_guardian` 행 삭제. **보호자 0명은 정상 상태다** |
  | 학부모의 자녀 확인 | `/admin/members`(학부모 상세) | `GET /api/members/{id}/students` | 읽기 전용. 비활성 학생도 함께 나온다 |

  ⚠️ **`B6` 의 화면 경로를 `FE-11` 실제 경로에 맞춘다.** 계획상 `/admin/members` 한 화면에 4탭이라, 기존 표의 `/admin/students`·`/admin/buses` 는 **실제로 만들어지지 않는다.** 안 만든 경로를 남겨두면 다음 사람이 찾다가 못 찾는다.

- [ ] **Step 7: `B6` 의 낡은 서술 3줄을 고친다.**
  - *"엔드포인트 15개가 전부 구현돼 있다 (tenant 4 · member 2 · bus 5 · student 6 …)"* → **member 2→6, student 6→9** 로 다시 센다
  - *"⚠ **배정 해제 수단이 없다.** … 한 번 배정하면 앱은커녕 API로도 해제할 수 없다"* → ⚠️ **보호자 연결에 한해서만 해제가 생겼다.** 버스·정류장 배정은 **여전히 해제 불가**다. 통째로 지우지 말고 범위를 좁힌다
  - *"DELETE 매핑이 0개다"* → **3개**(`/members/{id}` · `/students/{id}` · `/students/{id}/guardians/{guardianUserId}`)

- [ ] **Step 8: `B6` 를 A부로 승격하고 C장 갭을 정리한다.** 문서의 갱신 규칙(*"프론트에 새 화면·새 라우트가 생겼을 때 → 해당 항목을 B부에서 A부로 옮기고, C장의 해당 갭을 지운다"*)을 그대로 따른다.
  - `B6` → **`A8. 관리자 — 계정·매칭 관리`** 로 옮긴다. ⚠️ **표를 6열에서 A부의 5열(`# / 사용자 행동 / 화면 / 호출 API / 결과`)로 바꾼다** — A부는 역할이 고정이라 `사용자` 열이 없다
  - ⚠️ **B6 전체를 옮기지 않는다.** 학원 등록(`/platform/tenants`)·노선 마스터·버스 등록은 **여전히 화면이 없다.** 옮기는 것은 **구성원·학생·보호자 부분뿐**이고, 나머지는 B6 에 남긴다
  - `C1`(*"기초 데이터를 앱에서 만들 수 없다"*) — **부분 해소.** 회원·학생은 되고 학원·버스·노선은 안 된다. 항목을 지우지 말고 **범위를 좁힌다**
  - `C11`(*"계정을 만들 정상 경로가 없는데, 비정상 경로는 열려 있다"*) — 앞 절반은 A1 이, 뒤 절반은 `BE-11` 이 해소 → **삭제**
  - `C-1` 여정 ASCII 의 `[관리자 온보딩]` 줄에서 `회원 등록 ✗ → … 학생 등록 ✗` 를 `✅` 로 바꾸고, `C1: 여기서부터 앱으로 못 간다` 화살표를 **학원 등록 자리로 옮긴다**
  - B부 도입부의 *"프론트가 호출하는 REST 22개 / 백엔드 68개 / 사용률 32% … `ApiClient` 에는 `put`·`delete` 메서드가 아예 없다. 쓰기 호출은 … 7개뿐"* → **네 수치 전부 다시 센다**(§5 명령). `delete` 는 생겼다

**검증:** 각 플로우의 API 열이 `docs/API_SPEC.md` 의 실제 엔드포인트·권한과 일치하는지 대조한다.

---

## DOC-3: `docs/API_SPEC.md`

**목적:** 계약 문서를 실제 컨트롤러와 일치시킨다.
**Files:** Modify `docs/API_SPEC.md`

- [ ] **Step 1: 신규 엔드포인트 6개 추가** — OVERVIEW §4.2 목록 그대로(`/route-plans/simulate`·`/simulate/apply` · `/locations/children/buses` · `/location-change-requests` 3종). 각각 요청·응답·권한·에러·구현상태
- [ ] **Step 2: 권한 변경 7개 반영** — OVERVIEW §4.1 표 그대로. ⚠️ `GET /api/drive-sessions/bus/{busId}` 를 빠뜨리지 마라(선탑자 명단 사슬의 필수 칸)
- [ ] **Step 3: 확장된 응답 2개** — `GET /api/buses/{id}`(기사·선탑자·당일 계획·명단+보호자) · `PATCH /api/buses/{id}/assignment`(`attendantId`). `DriveSessionRosterEntry.photoUrl` 추가도 함께
- [ ] **Step 4: STOMP 목적지 2개** — `/topic/tenant/{id}/bus-locations` · `/user/queue/bus-location`
- [ ] **Step 5: enum 사전 갱신** — `Role`·`NotificationType`·`LocationChangeDecision`·`OverrideAction`
- [ ] **Step 6: 프론트 사용 여부 열 갱신** — 이번에 붙은 화면이 쓰는 엔드포인트를 "사용"으로 바꾼다

### 【인적 축】 A1·A2 추가분 — `DOC-3` Step 7~10

**항목 하나의 형식**(실측) — 새 8개를 이 6블록 그대로 쓴다. 열을 새로 만들지 않는다.

```
### `PATCH /api/students/{id}` — 학생 정보 수정  ✅프론트 사용   ← ① 소제목 + 프론트 사용 꼬리표

| | |                                                          ← ② 머리글 없는 2열 메타표, 행 3개 고정
|---|---|
| 권한 | 클래스 레벨 관리자 전용 |
| 테넌트 격리 | `TenantGuard.resolveTenantId(admin, student.tenantId)` |
| 구현 상태 | ✅ 구현 |

**요청** — path `id: Long`(필수)                               ← ③ body 는 `필드|타입|필수|설명` 4열 표

**응답** `200` — `StudentResponse`                             ← ④

**주의**                                                       ← ⑥ 결함·함정은 ⚠ 접두
- …
```
> 에러는 **전용 블록이 없다.** 상태코드는 ⑥ 주의 불릿이나 *"처리 순서와 실패 코드"* 번호 목록에 인라인으로 적는다.
> `PATCH /{id}/password` 는 `POST /api/members` 가 쓰는 **"처리 순서와 실패 코드"** 형식을 재사용하면 톤이 맞는다.

- [ ] **Step 7: §9 학생에 3개 + 쿼리 1개.** 배치는 문서의 **생명주기 순**(조회→생성→수정→파괴)을 지킨다.
  - `PATCH /api/students/{id}` → **`GET /{id}` 와 `PATCH /{id}/assignment` 사이**
  - `DELETE /api/students/{id}/guardians/{guardianUserId}` → **`POST .../guardians` 바로 뒤**(짝을 붙인다)
  - `DELETE /api/students/{id}` → **§9 맨 끝**(파괴적 동작을 마지막에)
  - `?includeInactive=` 는 신규 항목이 아니다 — `GET /api/students` 의 **요청 줄과 주의 줄을 고친다**
  - ⚠️ **함께 고치지 않으면 자기모순이 된다**: `StudentResponse(...)` 필드 목록에 **`active`·`phone`·`photoUrl`·`pickupAddress` 추가**, 주의의 *"페이징 없음(전건 반환)"* → **"기본은 활성 학생만"**
  - ⚠️ 보호자 연결 항목의 결함 2줄(*"역할이 `PARENT` 인지, 같은 학원인지 검증하지 않는다"* · *"중복 연결 방지가 없다 … 위반 시 `500` 으로 나온다"*)은 **`BE-14` 가 해소** → 주의에서 뺀다

- [ ] **Step 8: §11 회원에 5개.** 현재 2개뿐이라 §9 패턴(목록→등록→상세→수정→삭제)에 맞춘다.
  `GET /{id}` → `PATCH /{id}` → `PATCH /{id}/password` → `GET /{id}/students` → **`DELETE /{id}` 는 맨 끝**.
  - ⚠️ **`MemberResponse(userId, email, name, role, tenantId)` 에 `phone`·`photoUrl` 을 추가한다.** 지금 `phone` 은 요청에만 있고 `photoUrl` 은 **문서 전체에 0회** 등장한다
  - ⚠️ *"**구성원 CRUD 중 C·R 만 있다.** 수정(U)·삭제(D) 엔드포인트가 없어 기사 퇴사 처리, 역할 변경, 비밀번호 재설정을 API 로 할 수 없다. DB 직접 수정이 유일한 방법이다"* → **전면 재작성.** 이 한 줄이 A1 의 존재 이유였다

- [ ] **Step 9: 문서가 스스로 부정하게 되는 3곳.** ← **빠뜨리면 문서가 자기모순이 된다**
  - §1 공통 규약 *"`PUT` 과 `DELETE` 메서드를 쓰는 엔드포인트가 **전체에 0개**다. 수정은 전부 `PATCH`, 삭제 API 는 존재하지 않는다"* → **DELETE 3개**로 고치고, **셋 다 물리 삭제가 아님**을 함께 적는다
  - §1 상태코드 표의 `409` 행에 **기사·선탑자 중복 배정**(`BE-14`)을 더한다
  - `PATCH /api/buses/{id}/assignment` 의 주의에 **중복 배정 409** 추가. 기존 결함 *"`driverId` 를 존재 여부만 확인한다"* 는 **`BE-7` 이 해소** → 뺀다

  > 💡 **"행은 남는다"를 쓰는 말투** — 이 문서엔 논리 삭제 선례가 없다. 가장 가까운 관용구는 승하차 정정의
  > *"**원본을 덮어쓰지 않는다(append-only).**"* 다. 같은 굵은글씨 패턴으로 **"계정 행을 지우지 않는다(멤버십만 해제)"** 처럼 쓰면 톤이 맞는다.

- [ ] **Step 10: 부록 A 매트릭스와 집계표.**
  - 매트릭스에 **8행 추가**(학생 6→9, 회원 2→7)
  - 집계표 6개 값(`REST 총계 68` · `프론트 사용 22(32%)` · `프론트 미사용 46(68%)` · `✅ 60` · `⚠ 부분 8` · `스텁 0`)을 **§5 명령으로 다시 센다**
  - *"관리자 마스터데이터 관리(CRUD) | 17 | … 프론트 `ApiClient` 에 `put`/`delete` 메서드조차 없고 …"* → 개수·서술 갱신
  - ⚠️ Swagger 태그 관련 *"태그가 붙은 오퍼레이션은 총 18개다"* — 새 8개에 `@Operation(tags=…)` 를 붙였는지 **코드로 확인**하고 수를 맞춘다. 안 붙였으면 수치를 그대로 두고 그 사실을 적는다

**검증:** 앱을 띄우고 Swagger UI(`http://localhost:8080/swagger-ui/index.html`)와 한 줄씩 대조한다. ⚠️ Docker 가 필요하면 **사용자에게 요청**한다.

---

## DOC-4: `docs/ARCHITECTURE.md`

**목적:** 새 구조 3가지를 구조 문서에 남긴다.
**Files:** Modify `docs/ARCHITECTURE.md`

- [ ] **Step 1: 노선 시뮬레이션 엔진** — `RoutePlanComputer`(순수 계산) / `RoutePlanSimulationService`(비교) / `RoutingCommandService`(저장) 3분할과 **왜 나눴는지**(순환 의존 회피 + 관리자 비교와 학부모 자동판정이 같은 계산 경로로 합류해야 한다)를 적는다. 의존 방향은 `Command → Simulation → Computer` 한 방향
- [ ] **Step 2: 버스 위치 실시간 파이프라인** — `BusLocationCommandService` → `BusLocationUpdatedEvent` → Kafka `bus-location-updated` → `BusLocationPushConsumer` → 관리자 토픽·학부모 개인 큐. 기존 학생 위치 파이프라인과 나란히 그린다
- [ ] **Step 3: 역할 6개** — 인증·인가 절에 `ATTENDANT` 반영. 승하차 기록 인가가 "그 버스에 배정된 선탑자 본인"으로 좁아진 것도 적는다
- [ ] **Step 4: 아키텍처 리스크 갱신** — 해소된 항목(버스 위치 폴링)을 빼고, 새로 생긴 것을 넣는다. 최소 2개: ① 시뮬레이션이 외부 경로 API 를 요청당 최대 5회 호출한다(정원 25 기준) ② `Bus.attendant` 미배정 시 승하차 기록이 **전면 불가**하다(폴백을 두지 않기로 한 대가 — D-B)

### 【인적 축】 A1·A2 추가분 — `DOC-4` Step 5~8

- [ ] **Step 5: §7.3 의 시한폭탄 문장을 고친다. ← 이 문서에서 가장 시급하다**

  현재 §7.3 끝에 이렇게 적혀 있다(인용):
  > *"존재하지 않는 `student_id` 를 가진 `ride_event` 가 들어가도 DB는 막지 않고, 학생을 지워도 기록이 고아로 남는다. 이건 트레이드오프이지 실수가 아니다 — 다만 **삭제 API가 없어서(현재 `DELETE` 엔드포인트가 0개) 아직 문제가 드러나지 않은 상태다.**"*

  **A1·A2 착수 즉시 이 문장이 거짓이 된다.** 여기에 **D-O 를 답으로 붙인다** — *"삭제 API가 생겼지만 물리 삭제를 하지 않기로 해서 이 문제를 피했다"* 로 잇는다.
  ⚠️ **근거가 두 갈래임을 지켜서 쓴다**(실측):
  - `bus.driver_id` · `student_guardian` → **FK 제약이 있다.** 물리 삭제하면 **DB가 막는다**
  - `ride_event` → **FK가 없다**(평문 Long 참조). 물리 삭제해도 **DB는 안 막고 감사 추적만 조용히 끊긴다** ← 이쪽이 더 위험하다

  바로 다음 문단의 *"**이력을 지우지 않는 것**이 이 도메인의 일관된 원칙이다"* 는 **D-O 의 근거로 그대로 재사용**한다. 새 원칙을 만드는 게 아니라 **기존 원칙을 삭제 API 에까지 적용한 것**임을 드러낸다.

- [ ] **Step 6: §7.1 FK 표에 `bus.attendant_id` 행을 신설한다.**
  ⚠️ **`attendant`·`선탑자` 는 이 문서에 0회 등장한다**(실측). D-P 가 *"기사↔선탑자를 잇는 유일한 매개"* 로 지목한 필드가 구조 문서에 **없는 상태**다.
  - FK 표에 `User(ATTENDANT) → Bus | bus.attendant_id | 허용(미배차)` 추가 — `bus.driver_id` 행 바로 아래
  - §3.1 모듈 책임표의 `bus` 행 *"버스 등록·기사/노선 배차"* → **"기사/선탑자/노선 배차"**
  - §3.2 또는 §7.2 에 **D-P** 를 한 줄로: *전용 매칭 테이블을 만들지 않는다. 두 사람이 함께 탄다는 사실의 유일한 근거는 같은 `Bus` 행이다.* ⚠️ 이건 **기존 서술의 수정이 아니라 공백을 메우는 신규 서술**이다

- [ ] **Step 7: §5·§6 의 수치와 역할.**
  - §5.3 `Role` enum 5종 → **6종**(`ATTENDANT`). Step 3 이 이미 다루므로 **중복하지 않는다**
  - §5.3 역할별 접근 범위 표에 **`ATTENDANT` 행** 신설, `ACADEMY_ADMIN` 행의 클래스 레벨 `@PreAuthorize` 3곳 인용(`StudentController` · `BusController` · `MemberController`)에 **줄번호가 바뀌었는지** 확인
  - §6.3 *"호출 지점 실측 26개소(18개 서비스) — … bus 4 · user 2 …"* → **`BE-12`·`BE-14` 가 `TenantGuard` 호출을 늘린다.** 다시 센다
  - §5.3 *"정의된 역할 5개 중 화면이 있는 역할은 3개 … 관리자 각 4화면"* → 역할 6개·화면 보유 역할·관리자 5화면으로

- [ ] **Step 8: §12 리스크 — `R20` 부터 붙인다.** 현재 `R1`~`R19` 로 **결번이 없다**(실측). 포맷은 `**R{n}. 제목**` + `- 사실:` / `- 영향:` / `- 개선:` 3줄 고정.
  - ⚠️ **`R4`(signup 이 tenantId·role 을 그대로 신뢰) 는 지우지 말고 잇는다.** 그 개선안이 이미 *"관리자·기사 계정은 초대·승인 경로로만 만든다"* 라고 적혀 있어 **D-M 과 방향이 같다** — A1 이 그 경로를 만들었고 `BE-11` 이 엔드포인트를 닫았다는 연결을 명시한다
  - 신규 후보 2개(실측 근거 있음):
    - **비밀번호 재설정 후 기존 세션이 살아 있다** — 토큰 무효화 장치가 저장소에 없다(`BE-12` Step 7 이 인정한 한계)
    - **`ride_event` 는 FK가 없어 학생 비활성화가 기록을 보호하지 못한다** — `active=false` 는 조회에서만 빠지고, 누군가 DB에서 직접 지우면 여전히 고아가 된다
  - `R7`(버스 위치 폴링)은 **운행 축**(`BE-8`)이 해소한다 — Step 4 와 **중복 처리하지 않는다**

**검증:** 다이어그램·서술의 클래스명이 실제 파일과 일치하는지 확인한다.

---

## DOC-5: `CLAUDE.md` · `docs/README.md`

**목적:** 세션마다 읽히는 진입 문서를 현재 사실에 맞춘다.
**Files:** Modify `CLAUDE.md` · `docs/README.md`

- [ ] **Step 1: `CLAUDE.md` 마이그레이션 규칙 개정** — 현재 *"기존 `V1__init_schema.sql` 수정 금지"* → **개발 단계에서는 V1 을 직접 수정한다**(D-D). 반영하려면 `docker compose down` 후 `up` 이 필요하고 `stop`/`start` 로는 Flyway 체크섬 검증에 걸린다는 점을 함께 적는다
- [ ] **Step 2: `CLAUDE.md` 사용자 계층 문구** — "사용자 5계층" → **6계층**, 선탑자 추가
- [ ] **Step 3: `docs/README.md` §3 현황 요약 다시 쓰기** — 현재 *"화면이 있는 역할은 3개 / 전체 라우트 9개 / 프론트가 호출하는 엔드포인트 22개"* 를 **실측값으로** 갱신. 학생만 화면이 없다는 사실을 남긴다
- [ ] **Step 4: `docs/README.md` §4 문서 지도에 `docs/plans/` 한 줄 추가** — 계획서는 SoT 가 아니라는 점을 분명히 적는다

### 【인적 축】 A1·A2 추가분 — `DOC-5` Step 5~8

- [ ] **Step 5: `CLAUDE.md` 프론트 현황 — 관리자 4화면 → 5화면.**
  현재 문장(인용): *"프론트는 **기사 앱(2화면) + 관리자(4화면)** 까지 붙어 있다. **학생·학부모 화면은 아직 없다**"*
  → 선탑자·학부모 앱까지 생기므로 **통째로 다시 쓴다.** 학생만 화면이 없다.

- [ ] **Step 6: `CLAUDE.md` 시드 방침과 D-M 의 충돌을 명시한다. ← 안 적으면 반드시 혼란이 생긴다**

  현재 두 문장이 이렇게 돼 있다(인용):
  > *"로그인 계정은 Flyway 시드(`db/migration-local/V2__seed_data.sql`) 참조 — 비밀번호는 모두 `password`."*
  > *"**로컬 postgres는 의도적으로 영속 볼륨이 없다** … `docker compose down` 후 `up` 으로 다시 띄우면 Flyway가 … 매번 자동으로 새로 구성한다"*

  ⚠️ **이 둘과 D-M 이 정면으로 부딪힌다.** 관리자 화면에서 만든 계정은 **`down` 한 번에 전부 사라진다.**
  이건 결함이 아니라 로컬 리셋 정책의 당연한 결과지만, **적어 두지 않으면 개발자가 "계정 생성 기능이 고장났다"고 판단한다.**
  → *"관리자 화면에서 만든 계정은 로컬에서 `docker compose down` 시 사라진다. 반복 테스트에 쓸 계정은 시드에 넣는다"* 를 한 줄 추가한다.
  로그인 계정 설명도 **"시드 계정 + 관리자가 만든 계정"** 두 갈래로 고친다.

- [ ] **Step 7: `docs/README.md` §3 수치와 §4 문서 지도.**
  - §3 의 *"각 4화면"* · *"전체 라우트 **9개**, 프론트가 호출하는 REST 엔드포인트 **22개**"* → §5 명령으로 다시 센다
  - §3 의 *"백엔드는 … **기초데이터 관리**까지 구현돼 있으나 **프론트가 쓰지 않는다**"* → **부분적으로 거짓이 된다.** 구성원·학생은 쓰고, 학원·버스·노선은 여전히 안 쓴다
  - §4 문서 지도에 **`docs/plans/` 한 줄 추가**(Step 4 가 이미 지시) — ⚠️ **이건 이미 발생해 있는 drift 다.** `docs/plans/` 에 파일이 있는데 §1 은 *"이 폴더의 문서 4종"*, §5 는 *"이 폴더의 `.md` **4종이 원본**이다"* 라고 단언한다. **§1·§5 도 함께** 고쳐 `plans/` 가 SoT 가 아닌 예외임을 밝힌다

- [ ] **Step 8: 기존 drift 1건을 같이 고친다.** `CLAUDE.md` 는 구현 상태 기호를 **3개**(`✅ 구현 / 🟡 부분 / ⬜ 미구현`)로 적는데 `docs/README.md` §2 는 **4개**(`➖ 범위 밖` 포함)를 정의한다. `CLAUDE.md` 를 4개로 맞춘다.
  ⚠️ A1·A2 와 무관한 항목이지만 **같은 파일을 여는 김에 처리한다** — 따로 열면 잊는다.

**검증:** `CLAUDE.md` 를 처음 읽는 사람 관점에서 낡은 서술이 남았는지 훑는다.

---

## 5. 실측에 쓰는 명령

수치를 **추정하지 말고** 아래로 센다. 결과가 계획서와 달라도 **실측이 이긴다.**

```bash
# 백엔드 REST 엔드포인트 수
grep -rEo '@(Get|Post|Put|Patch|Delete)Mapping' backend/src/main/java --include=*.java | wc -l

# 컨트롤러 수
find backend/src/main/java -name '*Controller.java' | wc -l

# 프론트 라우트 수 (AppRoutes 의 static const)
grep -cE '^\s*static const' frontend/lib/app/router/app_routes.dart

# 프론트가 실제 호출하는 엔드포인트 (중복 제거)
grep -rhoE "'/api/[^']+'" frontend/lib | sort -u | wc -l

# 엔티티 수
grep -rl '@Entity' backend/src/main/java | wc -l

# 역할 수 (Role enum 상수)
grep -cE '^\s+[A-Z_]+,?\s*//' backend/src/main/java/src/backend/user/entity/Role.java

# 관리자 화면 수 (AppRoutes 의 /admin 경로)
grep -cE "static const admin\w* = '/admin" frontend/lib/app/router/app_routes.dart

# 활성 학생 필터가 남은 곳이 없는지 — 결과가 0 이어야 한다
grep -rn 'findByTenantId(' backend/src/main/java/src/backend/student backend/src/main/java/src/backend/routing
```

⚠️ **인적 축 수치는 착각하기 쉽다.** `/api/members` 는 **학생을 포함하지 않는다**(D-N — 학생은
`app_user` 가 아니라 `student` 테이블이다). "관리 가능한 계정 종류 4개"와 "구성원 API 가 다루는 역할 3개"를
같은 수로 적지 마라.

---

## 5.5 ⚠️ 같은 수치가 여러 문서에 복제돼 있다

**한 숫자를 고치면 최소 5곳을 함께 고쳐야 한다.** 한 곳만 고치면 문서끼리 어긋나고, `docs-drift-auditor` 가
"어느 쪽이 맞는지" 판단할 수 없게 된다. **수치를 고칠 때는 아래 표를 체크리스트로 쓴다.**

| 수치 | 복제된 곳 |
|---|---|
| **관리자 화면 수**(현재 4) | `CLAUDE.md` 저장소 소개 · `docs/README.md` §3 · `PRODUCT_SPEC` §2 역할표 2행·§11.1·§11.2 역할별 앱 · `ARCHITECTURE` §1.1·§5.3·§4.3 provider 계보표 |
| **프론트 라우트 수**(현재 9) | `docs/README.md` §3 · `PRODUCT_SPEC` §11.1 · `ARCHITECTURE` 부록 |
| **프론트 호출 엔드포인트 / 사용률**(현재 22 / 32%) | `docs/README.md` §3 · `PRODUCT_SPEC` §11.1 · `API_SPEC` 부록 A 집계표 · `USER_FLOWS` B부 도입부 · `ARCHITECTURE` §1.1·부록 |
| **백엔드 REST 총계**(현재 68) | `PRODUCT_SPEC` §11.1 · `API_SPEC` 부록 A 집계표 · `USER_FLOWS` B부 도입부 · `ARCHITECTURE` §1.1·부록 |
| **역할 수 / 화면 있는 역할**(현재 5 / 3) | `CLAUDE.md` 5계층 · `docs/README.md` §3 · `PRODUCT_SPEC` §2·§11.1 · `ARCHITECTURE` §5.3·부록 · `USER_FLOWS` §0.2 |
| **엔티티 수**(현재 16) | `PRODUCT_SPEC` §3.1 · `ARCHITECTURE` §7 제목·부록 |
| **`DELETE` 엔드포인트 0개** | `API_SPEC` §1 공통 규약 · `ARCHITECTURE` §7.3 · `USER_FLOWS` C장·B6 |

> 💡 **작업 순서 요령** — 문서를 하나씩 끝내지 말고, **수치 한 종류를 골라 전 문서에서 동시에** 고치는 편이 빠지지 않는다.

---

## 6. 마무리 검증

- [ ] `docs-drift-auditor` 에이전트를 **`docs/` 4종 전체**에 한 번 돌린다
- [ ] 지적 사항을 고치고, 고칠 수 없는 것(범위 밖)은 문서에 `⬜ 미구현` 또는 `※ 미확인:` 으로 **남긴다** — 조용히 지우지 않는다
- [ ] **§5.5 수치 체크리스트**를 한 줄씩 확인한다. 문서 간 같은 숫자가 다르면 실패다
- [ ] **`.html` 을 하나도 건드리지 않았는지 확인한다**(§0 공통 원칙). HTML 변환은 모든 개발이 끝난 뒤 별도 단계다
- [ ] `docs/plans/2026-08-02-mvp-확장-OVERVIEW.md` §5 진행표의 `DOC-*` 행을 완료로 표시한다
- [ ] 커밋(OVERVIEW §1.5) — `docs: MVP 확장 결과를 SoT 4종에 반영 [DOC-1~5]`

---

## 7. 이번 조사에서 발견한 **기존 drift 3건** (A1·A2 와 무관)

구현 결과를 반영하는 김에 같이 고친다. 따로 열면 잊는다.

| # | 내용 | 고칠 곳 |
|---|---|---|
| 1 | `docs/plans/` 디렉토리가 실재하는데 `docs/README.md` §1·§4·§5 가 *"이 폴더의 문서 4종"*·*"`.md` 4종이 원본"* 이라고 단언한다 | `DOC-5` Step 7 |
| 2 | `CLAUDE.md` 는 구현 상태 기호를 3개로, `docs/README.md` §2 는 4개로 정의한다 | `DOC-5` Step 8 |
| 3 | `PRODUCT_SPEC` 이 `(→ 12장 F-3)` 으로 가리키는 대상이 실제 `F3`(학원 1번 폴백 하드코딩)과 다르고, 표기도 `F-3`/`F3` 로 어긋난다 | `DOC-1` Step 10 — ⚠️ **갭 번호를 재배열하기 전에** 고친다 |
