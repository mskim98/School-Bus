# OS
.DS_Store

# IDE
.idea/
*.iml

# Claude Code local (machine-specific overrides, not shared)
.claude/settings.local.json

# tokensave local cache db
.tokensave/
**/.tokensave/

# Backend (Gradle / Spring Boot)
backend/build/
backend/.gradle/
backend/out/
backend/HELP.md
!backend/gradle/wrapper/gradle-wrapper.jar
backend/.env

# 배포 (운영 시크릿 — 커밋 금지)
# Swagger UI basic auth 계정 파일. 작업 트리에 만들지 않고 EC2 에서 직접 생성한다
# (절차: docs/DEPLOYMENT.md §2.12). 실수로 만들어졌을 때를 대비해 여기서도 막아 둔다.
infra/proxy/.htpasswd

# Frontend (Next.js)
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env.local
frontend/npm-debug.log*

# Root
node_modules/

# 문서(docs/) 전부 추적하지 않음 — 루트 · backend · frontend (2026-09-02 사용자 확정)
# 로컬 파일은 그대로 두고 git 에만 올리지 않는다. ⚠ git worktree 에는 복사되지 않으므로 워크트리 에이전트에는 메인 저장소 절대 경로를 준다
**/docs/
