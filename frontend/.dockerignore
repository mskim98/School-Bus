# 빌드 컨텍스트에서 제외 — 컨테이너 안에서 flutter pub get / build 를 다시 돌리므로
# 호스트의 빌드 산출물·툴 캐시를 넣을 이유가 없다(컨텍스트만 커지고 캐시가 깨진다).
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# 플랫폼 런너 — Web 빌드에는 불필요
android/
ios/
macos/
linux/
windows/

.idea/
.vscode/
*.iml
.git/
.gitignore
docs/
