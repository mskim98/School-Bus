# 세션 인수인계 — 2026-08-25 (Phase 2 진행 중)

## ⓪ 이 세션의 종료 지점 (사용자 지시)

⚠ **2026-08-25 갱신 — 종료 지점이 "Phase 2 완료 시" 로 바뀌었다.** 앞선 "T3 완료 후 중단" 지시를 사용자가 대체했다. 따라서 이 문서의 아래 서술 중 "T3 에서 끊는다" 를 전제한 부분은 낡았다. **현재 범위는 T4·T5·완료 조건 11항까지다**(Ruling 108).


**Phase 2 완료 시점에 중단**한다 (Ruling 81). Phase 3 으로 자동 진행하지 않는다.

- Ruling 53(Phase 1 종료 후 중단) → Ruling 66 으로 철회 → **Ruling 81 로 Phase 2 종료 시점 재지정**
- **Ruling 42(연속 진행)는 폐기가 아니다** — 다음 세션은 재개 시 다시 연속 진행이 기본
- 완료 판정은 `p2-goal-table.md` **11항 전건 통과**. 부분 통과는 🟡
- 중단 시 **`pkill caffeinate`** 로 절전 억제를 해제할 것 (조율자가 `sleep 1` 대응으로 띄워 둠)

---

## 0. 다음 세션이 가장 먼저 할 일

**`.superpowers/sdd/IMPLEMENTATION_PLAN/progress.md` 를 읽는다.** 그것이 회복 지도이고 이 문서보다 상세하다. 이 문서는 그 원장으로 가는 포인터이며, 원장이 담지 않는 **"지금 무엇이 공중에 떠 있는가"** 만 적는다.

읽는 순서 — `progress.md` → `docs/IMPLEMENTATION_PLAN.md §8`(진행 표) → Phase 1 절 → `§4.6`·`§4.7` → `§7` 횡단 규칙 22개 → `CLAUDE.md`.

## 1. 어디까지 왔나

**Phase 0 ✅ · Phase 1 ✅ · Phase 2 🟡(부분 완료).**

### Phase 1 — 완료 (조건 6항 전건 실증)

엔티티 **39/39** · 테이블 39 · 시드 · `SeedFixtures` · Swagger 계정표. 두 독립 측정(검증 에이전트 + 조율자 재실측)이 6항 전부 일치.

### Phase 2 — 인증 · 계정 상태 게이트 · RBAC · 학원 격리

목표 표는 `.superpowers/sdd/IMPLEMENTATION_PLAN/p2-goal-table.md` 에 **완료 조건 11항**으로 고정돼 있다. **아직 실증하지 않았다.**

| 태스크 | 상태 | 산출물 |
|:-:|---|---|
| T1 인가 어휘 | ✅ 리뷰 통과 | 권한 **31종** · 역할↔권한 부여표 · 애너테이션 4종(`@CanRegisterDevice`·`@PublicEndpoint`·`@AuthenticatedOnly`) |
| T2 계정 상태 게이트 | ✅ 리뷰·재리뷰 2회 통과 (Critical 1 + Important 10 + Minor 3 전건 해소) | 인터셉터 · `@AllowedWhenPending`/`@AllowedWhenRejected` · `ErrorResponse` 신설 · **인증 소거 선재 결함 수정** |
| **T3 가입·프로필·저장소** | **← 이 세션의 마지막 태스크. 상태는 아래 §2.4 참조** | §2.1~2.4·2.10·2.11 + `account/repository/` 완성 |
| T4 로그인·토큰·쿠키 | ⬜ **미착수** | 브리프 `p2-task-4-brief.md` 완료 |
| T5 학원 격리 | ⬜ **미착수** | 브리프 `p2-task-5-brief.md` 완료 |

**브리프·규약이 전부 선작성돼 있다** — `p2-task-{1..5}-brief.md` · **`p2-controller-conventions.md`**(T3·4·5 공통 규약, 브리프가 복제하지 않고 가리킴).

⚠ **예정된 RED 3건이 스위트에 있다** — 컨트롤러가 0개라서 나는 것이고 **코드 결함이 아니다.**
`검사_대상_핸들러가_하나도_없으면_실패한다()` · `PublicEndpoint_가_붙은_엔드포인트_집합이_허용목록과_정확히_일치한다()` · `허용_애너테이션이_붙은_실제_핸들러_수가_pending_2개_rejected_3개다()`
**T3·T4 가 컨트롤러를 만들면 GREEN 으로 전환된다. 전환 여부가 그 태스크들의 완료 판정의 일부다.**

## 2. ⚠ 공중에 떠 있는 것 · 다음 세션이 먼저 볼 것

### 2.1 다음 착수점 — Phase 2 Task 4 · Task 5 (병렬)

**T4 와 T5 는 동시에 띄울 수 있다.** T3 이 `account/repository/` 를 완성하도록 범위를 넓혔기 때문에 T4 는 저장소를 건드리지 않고 T5 가 배타적으로 소유한다(Ruling 78 의 의존 경로 분석을 착수 전에 적용한 결과).

워크트리는 `git worktree add -b <브랜치> <경로> <현재 HEAD>` 로 **조율자가 직접 만든다** — 자동 격리(`isolation: "worktree"`)는 **206커밋 전에서 갈린 전례가 있어 신뢰하지 않는다**(Ruling 41).

### 2.2 Phase 2 완료 조건 11항 미실증

`p2-goal-table.md` 를 `goal-verifier` 에이전트로 실행한다. Phase 1 에서 같은 방식으로 6항을 실증한 선례가 원장에 있다.

### 2.3 T3 이 남긴 선행 조치 3건의 상태

브리프 §2 가 요구한 것 — ①`SecurityConfig.roleHierarchy()` 배선(**안 하면 모든 권한 애너테이션이 403**) ②`API_SPEC` 경로 문자열 그대로 사용(**어긋나면 "인가 오류" 로 보이는 실패**) ③**`@SpringBootTest` 간헐 실패 해소**(Ruling 89 — 완료 조건 판정이 전부 스위트에 걸려 있어 흔들리면 신뢰 불가). **T3 보고서에서 이행 여부를 확인할 것.**

### 2.4 T3 결과

**커밋 `07b8499`**(47파일 1278+/20-). 조율자 실측 **41클래스 · 212테스트 · 실패 2**.

- ✅ 담당 6개 엔드포인트(§2.1~2.4·2.10·2.11) 전부 구현, 목표 6문장 RED→GREEN
- ✅ **예정 RED 1건이 GREEN 전환** — `검사_대상_핸들러가_하나도_없으면_실패한다()`. **T1 의 인가 어휘가 실제로 맞물린다는 증거**
- ⚠ **남은 예정 RED 2건은 T4 가 채워야 GREEN** — `PublicEndpoint_가_붙은_엔드포인트_집합이_허용목록과_정확히_일치한다()`(login·refresh·recover 3개 미부착) · `허용_애너테이션이_붙은_실제_핸들러_수가...`(로그아웃 미부착). **코드 결함이 아니다**
- ✅ 저장소 계층 완성 — `AccountRepository`·`RefreshTokenRepository`·`VerificationCodeRepository` 등 8개. **T4 는 저장소를 건드리지 않고 T5 가 배타 소유**
- ⚠ **Phase 1 소유 테스트 10개를 수정** — 리포지토리 도입으로 컨텍스트 로딩 실패 41건이 나 `excludeAutoConfiguration = DataJpaRepositoriesAutoConfiguration.class` 추가. 원인은 **`@EntityScan` 이 엔티티만 좁히고 리포지토리 자동 스캔은 안 좁히는 것**. 구현자가 자기 점검 #10 에서 "아니오" 로 정직하게 보고
- **리뷰 판정 파일** — `.superpowers/sdd/IMPLEMENTATION_PLAN/p2-review-t3-verdict.md` ⚠ **조율자가 이 판정을 읽고 마쳤는지는 §6 을 볼 것.** 미해소 지적이 있으면 **T4 착수 전에 처리한다**

### 2.4.1 ⚠ T4 가 반드시 반영할 것 — 사양 정정(Ruling 98)

조율자가 `API_SPEC §1.4` 를 고쳤다(커밋 `f61506a`). **`pending` 허용이 2개 → 4엔드포인트**로 늘었다 — `GET /auth/signup-status` · `POST /auth/logout` · **`GET /me`** · **`POST`·`DELETE /me/devices`**.

근거 — `§2.10` 이 "전 역할 공통이며 `pending`·`rejected` 도 호출 가능 — 대기 화면이 상태를 알아야 함", `§2.11` 이 "인증된 전 역할(`pending` 포함 — 승인 결과 알림이 대상)" 을 명시한다. **`/me` 가 없으면 앱 재실행 후 `role`·`status` 재취득 수단이 부재해 대기 화면 분기가 성립하지 않고, `/me/devices` 가 없으면 승인 결과 푸시를 받을 단말이 미등록된다.**

**코드는 아직 안 고쳤다. T4 가 할 일 —**
1. `GET /me` · `POST`·`DELETE /me/devices` 에 **`@AllowedWhenPending` 부착**
2. `AccountStatusGateInterceptorTest` 의 개수 단언(**"pending 2개 · rejected 3개"**)을 새 목록에 맞춰 수정
3. **`MeControllerTest.pending_계정이_me_를_부르면_403_AUTH_PENDING_이다` 는 정정된 사양과 어긋난다** — 200 을 기대하도록 뒤집어야 한다

### 2.5 최종 전체 리뷰 이월 (Phase 1·2 누적)

Phase 1 — T2 Minor 1(`hasFailed()` 원인 미특정) · Minor 5(섹션 분할 2방식 + `substring(0,-1)`) · Minor 6(판정 전 커넥션 개방) · Ruling 51(겹⑤ 소속 서술) · Ruling 61(파싱 하한 가드) · T5 Minor 2건 · **조율자 직접 편집 2건**(`V1:183` guardian FK · `ERD §4.1` 행 분리 — **조율자 수정은 리뷰를 건너뛰므로 명시적 확인 필요**)
Phase 2 — `ControllerAuthorizationConventionTest.authzAnnotationNames()` 가 `authz/` 의 `.java` 를 전부 애너테이션으로 취급

### 2.6 후속 Phase 로 등재된 것

- **Phase 10** — 계정 상태 게이트가 **STOMP 축을 덮지 않는다**(`pending` 토큰으로 WS 세션 수립 가능). Phase 10 절에 완료 조건으로 등재 완료(Ruling 87)
- **Phase 6** — `resilience4j-spring-boot3`(Boot 3 아티팩트)의 Boot 4 호환성. 현 판정은 "클래스패스 무해" 에 한정
- **Task 4 브리프에 명시할 것** — `assertNotBlocked()` 호출 순서가 계정 존재를 노출(비밀번호 대조 **이후**로 옮길지 판정)

## 3. 이 세션에서 확정한 것 (Ruling 26~52)

전문은 `progress.md` 에 있다. **다음 세션이 모르면 손해 보는 것만** 추린다.

| # | 내용 |
|:-:|---|
| 26 | 조사 문서 `phase1-*.md` 는 **재료이지 사양이 아니다.** `docs/ERD.md` 가 이긴다. 낡은 값 3건이 남아 있음(`account.status` 대문자 등) |
| 30 | **전 엔티티가 FK 를 `Long` 원시 필드로 보유.** `@ManyToOne`·`@OneToOne`·`@MapsId` 예외 없이 금지 |
| 32 | `@Enumerated` 금지. enum 파일 안의 중첩 `@Converter` 정적 클래스(`X.Db`)로 연결 |
| 40 | **Phase 착수 전에 완료 조건을 "실행 명령·단언" 표로 원장에 먼저 박는다** (사용자 상시 지시) |
| 41 | **`isolation: "worktree"` 를 신뢰하지 않는다.** 조율자가 `git worktree add -b <브랜치> <경로> <커밋>` 으로 직접 만든다 — 자동 격리가 206커밋 전에서 갈린 사고 발생 |
| 42 | **Phase 를 연속 진행한다.** 확인을 묻지 않음 (사용자 상시 지시) |
| 47 | 세션 중단(워치독)의 원인 가설로 **대용량 텍스트 로딩**을 채택 — 전 브리프에 분할 읽기·10분 침묵 금지 명시. **확정된 원인이 아니라 가설이며 재발하면 바꾼다** |
| 49 | 스키마가 `GENERATED ALWAYS AS IDENTITY`(36개)라 시드는 **`OVERRIDING SYSTEM VALUE`** 를 쓰고 `pg_get_serial_sequence` 로 `setval` |
| 52 | 전역과 이름이 겹치는 프로젝트 에이전트를 만들지 않는다 — 겹치면 **교체**(병합 아님) |

## 4. Phase 1 완료 조건 6개 (§4.7 목표 표)

원장에 실행 명령·단언과 함께 표로 있다. 요지 —
1. 기동 시 **39** 테이블 + 시드 적재 · 2. `ddl-auto: validate` 통과 (**엔티티 39개 전부 매핑** — 개수를 소스에서 따로 셀 것) · 3. 재기동 시 시드 초기화 · 4. `prod`·`demo` 가드 테스트 · 5. `SeedFixturesContractTest` · 6. Swagger 최상단 시드 계정표

**6항 전부를 통과해야 `docs/IMPLEMENTATION_PLAN.md §8` 을 ✅ 로 바꾼다.** 부분 통과는 🟡 이고 미통과 항목을 비고에 적는다.


---

## 5. ⚠ Ruling 98 이후 확정분 (2026-08-25 후반) — 위 서술보다 이쪽이 우선

이 문서의 §2.4.1 은 pending 허용 목록을 **"4개"** 로 적었다. **틀렸다.**

### 5.1 허용 목록 실측 (Ruling 107)

`POST`·`DELETE /me/devices` 는 **핸들러 2개**다(`DELETE` 는 `/me/devices/{token}`).

- `pending` **5개** — `GET /auth/signup-status` · `POST /auth/logout` · `GET /me` · `POST /me/devices` · `DELETE /me/devices/{token}`
- `rejected` **6개** — 위 5개 + `POST /auth/signup/reapply`. 거부 코드는 `AUTH_PENDING` 이 아니라 **`AUTH_REJECTED`**

`p2-goal-table.md` 조건 2·3 과 `docs/IMPLEMENTATION_PLAN.md` 완료 조건을 이 값으로 갱신 완료(커밋 `bda53bb`).

### 5.2 조율자 결정 6건 (Ruling 99·102~106)

| # | 결정 | 한 줄 근거 |
|:-:|---|---|
| 99 | §2.7 단건 무효화(`revokeByTokenHash`)를 T3 이 선점 | §2.7 은 **요청 토큰 1건만**, §2.8 만 전량. 합치면 단말 A 로그아웃이 단말 B 를 끊는다 |
| 102 | 베이스 경로는 `addPathPrefix`, **`context-path` 아님** | 후자는 actuator·ws·swagger 까지 옮겨 배포 구성 5곳이 깨진다 |
| 103 | 공개 경로 **3자 대조** (프로덕션 상수 · 애너테이션 실측 · 테스트 하드코딩 목록) | 테스트가 프로덕션 상수를 읽으면 "승인 없이 공개 엔드포인트 추가" 를 못 잡는다 |
| 104 | 전역 snake_case (`spring.jackson.property-naming-strategy`) | 수동 `@JsonProperty` 는 하나만 빠뜨려도 컴파일·테스트가 못 잡는다 |
| 105 | 사양 §8 사전에 없는 에러 코드 삭제 | `INVALID_INPUT`·`NOT_FOUND` 부재 실측. 계정 부재는 `ACCOUNT_NOT_FOUND` |
| 107 | 허용 목록 개수 정정 (위 5.1) | 개수 단언이 이 값을 그대로 쓴다 |

**쿠키 `Path` 가 `/api/auth` → `/api/v1/auth` 로 바뀌었다**(커밋 `c792934`, 문서 3종 동기화). 옛 값을 쓰면 웹 토큰 갱신이 전부 실패한다.

### 5.3 T3 게이트 리뷰 판정

`p2-review-t3-verdict.md` — **사양 준수 ❌** · Critical **0** · Important **6** · Minor **10**. 수정 라운드 1 발주 완료(지시서 `p2-task-3-fix1-brief.md`, 지적 전건 + 조율자 결정 3건 포함).

⚠ **리뷰가 보지 않은 커밋이 있다**(Ruling 101) — 저장소 계층(`7b93515`)·§2.7(`36ca6ae`)·수정 라운드 1. **보완 리뷰 지시서를 `p2-review-t3-supp-instructions.md` 에 준비해 뒀다.** 리뷰 없이 T3 를 완료로 세지 않는다.

### 5.4 T4·T5 파일 소유표

`SecurityConfig` · `PublicEndpoints` · `ControllerAuthorizationConventionTest` 는 **양쪽 모두 금지**(T3 fix1 이 확정). 전체 표는 원장 Ruling 108.
