# 세션 인수인계 — 2026-08-11

다른 세션이 이 문서만 읽고 이어받을 수 있도록 정리한다. **먼저 읽을 것 3개**: 이 문서 → `.superpowers/sdd/2026-08-10-mvp-배포-준비/progress.md`(배포 원장) → `docs/README.md`(제품 SoT 진입점).

- 브랜치: `feat/mvp-deployment` (HEAD `7d81425`, 분기점 `feat/mvp-expansion-backend`@`49c0dd9`)
- 상태: **미push·미머지.** 원격에 없고 이 노트북에만 존재
- 백엔드 테스트: 253개 전부 통과 (`cd backend && ./gradlew test`)

---

## 1. 이번 세션에서 한 일

배포 준비 계획(13태스크)은 이전 세션에서 이미 끝났고, **의도적으로 미뤄 둔 후속 2건**만 남아 있던 상태였다. 그 2건을 처리했다.

### 커밋 `528c979` — 인증서 발급이 도메인 A 레코드 연결보다 먼저 오던 순서 결함

Let's Encrypt 의 HTTP-01 챌린지는 **Let's Encrypt 서버가 `api.<도메인>` 을 직접 조회해 EIP 의 80 포트로 접속**하는 방식이다. 그런데 절차서가 "인증서 발급(§2.9) → 도메인 연결(§2.10)" 순서라, 그대로 따르면 A 레코드 없이 발급을 시도해 **최초 발급이 실패**한다. §2.1 순서 요약도 같은 순서라 교차 확인으로도 안 걸렸다.

| 파일 | 변경 |
|---|---|
| `docs/DEPLOYMENT.md` | §2.9↔§2.10 교체(**도메인 연결 → 인증서 발급**). §2.9 에 선행 이유 + `dig +short` 전파 확인 추가. §2.10 에 전제 문장 추가. 옛 §2.9 의 "같은 SSM 세션 안에서" 는 사이에 DNS 콘솔 작업이 끼면서 전제가 깨져 "§2.8 과 같은 방법으로 접속해" 로 교체. §2.1 요약 순서 교체 + "중요한 지점 둘 → 셋" |
| `infra/scripts/bootstrap-ec2.sh` | 완료 안내 3단계 → 4단계. A 레코드 연결을 인증서 앞에 삽입 |
| `docs/superpowers/plans/2026-08-10-mvp-배포-준비.md` | 이 브랜치 관례대로 Task 7 블록 동반 수정. python 정규식 추출 후 **바이트 단위 일치 재확인** |

§2.11~§2.14 번호는 불변이라 `init-cert.sh`·`nginx.prod.conf`·`deploy-backend.yml` 의 상호참조는 미변경.

**미채택 결정**: `init-cert.sh` 에 DNS 선행 검사 추가. 문서·스크립트 안내가 순서를 명시하는 이상, resolver 캐시·NXDOMAIN 오탐으로 정상 발급을 막을 위험이 편익보다 크다고 판단.

### 커밋 `7d81425` — `docs/` SoT 4종에 `demo` 프로파일 반영

CLAUDE.md 가 `docs/` 를 단일 소스로 규정하는데, 정작 SoT 는 `demo` 프로파일 신설 이전 상태로 남아 "시드는 local 에서만"·"비밀번호는 전부 `password`" 라고 적혀 있었다. **배포 환경 비밀번호를 `password` 로 오해할 소지**가 있는 서술.

- `API_SPEC.md` §18 — 적용 범위를 `local`·`demo` 로, 계정 표 제목을 "`local` 에서만 `password`" 로. `seedPasswordHash` placeholder 경고 블록 추가
- `USER_FLOWS.md` §0.5 — 같은 취지로 갱신
- `ARCHITECTURE.md` §10 — `flyway.locations` 행·`migration-local` 행 정정
- `ARCHITECTURE.md` §11.3 — 프로파일 표를 **3개 → 4개**로. `demo` 행 신설(`prod` 대비 차이 2가지 + 기본값을 두지 않는 이유)
- 잔여 드리프트 2건 동반 정정 — `ARCHITECTURE.md` R19 · `PRODUCT_SPEC.md` M4 의 `ENABLE_QUICK_LOGIN` 서술이 "배포 파이프라인이 생기면 고정한다" 는 미래형이었으나, `deploy-web.yml:44` 가 이미 `--dart-define=ENABLE_QUICK_LOGIN=false` 고정. 남은 노출은 로컬 `docker-compose.yml:99` 뿐이라 수용 범위로 기록
- `backend/src/main/resources/application.yml:21` 주석 "local → local·demo"

### 검증

- `./gradlew test` → BUILD SUCCESSFUL (253 tests, 실패 0)
- `docs/DEPLOYMENT.md` 어투 위반 grep 0건 / §2 헤딩 번호 2.1~2.14 연속성 확인
- 계획 원문 ↔ `bootstrap-ec2.sh` 바이트 일치

---

## 2. 🔴 다음 세션이 반드시 알아야 할 것 — SoT `PRODUCT_SPEC.md` §12 가 낡음

`docs/PRODUCT_SPEC.md` 는 **2026-08-01**(커밋 `30ca3ba`) 작성. 백엔드 확장 작업 BE-1~14 는 **08-02** 완료(`f84da92`). 즉 **문서가 그 작업 이전 스냅샷**이고, §12 미구현·갭 목록 28건 중 일부는 이미 해소돼 있다. 이번 세션에 2건을 코드로 직접 확인했다.

| §12 서술 | 실측 결과 | 판정 |
|---|---|---|
| "회원가입이 body 의 역할을 그대로 신뢰 → 임의 학원에 학원관리자 self-signup 가능. **최우선 차단 대상**"(§12.4, §12.5 **2순위**) | `AuthCommandService.signup():48` 이 `ADMIN_GRANTED_ROLES` 를 차단. 전용 테스트 4개 존재(`signup_asAcademyAdmin_throwsForbidden` 등) | **해소 완료 — 문서가 틀림** |
| "정류장 목록 조회가 인증 주체를 안 받아 타 학원 정류장 열람 가능"(§12.4) | `RouteController.stops():62` 가 여전히 `AuthUser` 미수신. 단 :60 주석은 "정류장은 개인정보가 아니라 인증만 되면 조회 허용" 이라는 **의도적 결정**으로 서술 | 코드 그대로 · 문서는 "누락" · 주석은 "의도" — **셋이 어긋남. 판단 필요** |

**미실측**: §12.4 나머지 5건(WebSocket 역할 검사·정정 권한 범위·참조 무결성·입력 검증·N+1), F12(수정·삭제 API 부재 — BE-13 학생 수정·퇴원, BE-14 보호자 연결 해제가 08-02 에 들어갔으므로 부분 해소 가능성 높음).

> **→ 다음 작업의 첫 순서로 `docs-drift-auditor` 를 §12 전체에 돌릴 것.** §12 를 그대로 믿고 계획을 세우면 이미 끝난 일을 다시 하게 된다.

---

## 3. 남은 작업

### 3-1. 배포 브랜치 닫기 (실행만 남음)

문서·스크립트 차단 요소는 이번 세션에 전부 제거. 남은 건 **실 AWS 에서만 드러나는 것**뿐이다.

1. `docs/DEPLOYMENT.md` §2 를 위에서부터 그대로 따라 실제 배포
2. 첫 배포에서 실증될 5개 — **C2**(CloudWatch Logs IAM 권한 충분 여부) · **I1**(AL2023 cronie) · **I3**(init-cert 안내) · **I6**(NAVER 키 필수 등록) · **I7**(§7 sudo)
3. 거기서 나온 수정을 **같은 브랜치에 담고** 그 뒤 `main` 으로 일괄 머지 — 사용자 결정 사항이며, 배포 산출물은 실행해 봐야 맞는지 알기 때문
4. deferred minor 약 20건 — 원장 `progress.md` 에 전부 기록. 머지 전 취사선택

⚠️ **원장 폴더 `.superpowers/sdd/2026-08-10-mvp-배포-준비/` 를 지우지 말 것**(머지 시점까지 유지). `.gitignore` 대상이라 커밋에 안 들어간다.

### 3-2. 제품 MVP — 프론트가 병목

백엔드는 14개 도메인이 다 있으나 **프론트 화면은 7개**(2026-08-11 실측: 로그인 1 + 관리자 4 + 기사 2). **학생·학부모 화면 0개** — 5계층 중 2계층이 로그인 직후 "준비 중" 배너에서 정지.

`docs/PRODUCT_SPEC.md` §12.5 가 정한 최소 항목 (2순위는 위 §2 대로 이미 해소):

| 순위 | 항목 | 성격 |
|---|---|---|
| 1 | **O4/F15** 관리자 데이터 관리 화면(버스·학생·회원 등록) | 프론트. 없으면 신규 학원 온보딩 자체가 불가 — 나머지 전부의 전제 |
| ~~2~~ | ~~회원가입 권한 상승 차단~~ | **✅ 실측 확인 — 완료** |
| 3 | **O1** 학부모 화면(자녀 위치·승하차 기록·알림함) | 프론트 전용. 백엔드 완비라 비용 대비 효과 최대 |
| 4 | **O5/F8** 위치 출처를 실 GPS 기본화 + 출처 정직 기록 | 프론트 + 백엔드 |
| 5 | **O2** SOS 발신·확인 화면 | 프론트. 백엔드 6개 엔드포인트 + 3분 에스컬레이션 스케줄러가 **아무도 안 부르는 채로 가동 중** |
| 6 | **F13** 승하차 정정 UI | 프론트. 백엔드 API 존재 |
| 7 | **F11** 서버 측 승하차 순서 검증 | 백엔드 |
| 8 | **F7** 시각 타임존 정상화(`LocalDateTime` → 타임존 포함) | 백엔드 |

### 3-3. 명시적 보류

- **Phase 8 실 GPS 전환** — 플래그 토글은 준비 완료. 법적 선행요건(위치정보 동의 UI·이력 스키마, `consent` 모듈)이 걸려 **실제 모바일 GPS 연동 테스트 시작 시점까지 보류**(2026-07-20 확정)
- `APPROACH`·`NO_SHOW` 알림 트리거 — Phase 7 이후로 보류

---

## 4. 권장 진행 순서

1. **`docs-drift-auditor` 로 `PRODUCT_SPEC.md` §12 재대조** — 계획의 근거를 먼저 신뢰 가능하게 만든다
2. **실 AWS 배포** — 배포 브랜치를 닫는다. 오래 둘수록 `main` 과 벌어진다
3. **O4(관리자 CRUD 화면)** — MVP 최소 항목 1순위

## 5. 이 저장소에서 조심할 것

- Docker 가 필요하면 **직접 `docker compose up` 하지 말고 사용자에게 먼저 요청**한다. Docker 꺼짐으로 인한 실패는 코드 결함이 아니라 환경 문제로 분류해 보고
- 로컬 postgres 에는 영속 볼륨이 부재(의도). 리셋은 반드시 `down` 을 거친다 — `stop`/`start` 는 데이터 유지
- 배포 환경(`demo`) 비밀번호는 `password` 가 **아니다**. SSM 주입값
- 사실이 문서 간 어긋나면 **`docs/` 가 기준**. 단 §2 에서 지적한 대로 §12 는 현재 낡은 상태
