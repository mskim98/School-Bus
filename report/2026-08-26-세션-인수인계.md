# 세션 인수인계 — 2026-08-26 (Phase 3 완료)

## ⓪ 이 문서의 성격

**원장으로 가는 포인터이지 복제가 아니다.** 다음 세션은 이 대화를 보지 못한다.

**가장 먼저 읽을 것 — `.superpowers/sdd/IMPLEMENTATION_PLAN/progress.md` 의 끝부분**(`tail -250`). 파일이 260KB 를 넘으므로 **전체를 읽지 마라.** Phase 3 절은 `# ▶ Phase 3 착수` 부터다.

읽는 순서 — `progress.md` 끝 → `docs/IMPLEMENTATION_PLAN.md §8`(진행 표) → **Phase 4·5 절** → `§4.6`·`§4.7` → `§7` 횡단 규칙 24개 → `CLAUDE.md`.

## 1. 어디까지 왔나

**Phase 0 ✅ · 1 ✅ · 2 ✅ · 3 ✅.** 다음은 **Phase 4 ‖ Phase 5**(§8.1 이 "확정 — 병렬 가능" 으로 판정).

브랜치 `feat/baraeda-rebuild`, tip `93773ab`. **push 부재.**

### Phase 3 결과

완료 조건 **7항 전건 실증** — 근거는 `.superpowers/sdd/IMPLEMENTATION_PLAN/p3-completion-evidence.md`(조건마다 "어느 단언이 **왜** 그 조건을 실증하는가" 를 단언 본문으로 확인).

| 항목 | 값 |
|---|---|
| 태스크 | 4개(T1 학원 CRUD · T2 가입 승인 · T3 계정 관리 · T4 종단) + 수정 라운드 3회 + 게이트 리뷰 2회 |
| 커밋 | `0070870..93773ab` **20개** · 122파일 7168+/212− |
| 엔드포인트 | **12개 신설**(누적 프로덕션 핸들러 **24** = Phase 2 의 12 + Phase 3 의 12) |
| `ErrorCode` | 15 → **25종** |
| 선행 작업 | Lombok `@RequiredArgsConstructor` 전환 32개 중 **25개**(사용자 지시) |

**음성 대조로 발견한 실제 결함** — T1 2건 · T2 2건 · T3 1건. **구현자 보고도, 테스트 개수 증가도, 초록 빌드도 그중 하나도 잡지 못했다.**

## 2. ⚠ 다음 세션이 먼저 알아야 할 것

### 2.1 다음 착수점 — Phase 4 ‖ Phase 5 (병렬)

`§8.1` 이 "**확정 — 병렬 가능**" 으로 판정해 뒀다(Phase 5 절이 "선행 Phase 3, **Phase 4 는 알림 발행에만 필요**" 를 명시).

**착수 전에 `p3-goal-table.md` 형식으로 Phase 4·5 목표 표를 각각 원장에 먼저 박는다**(Ruling 40, 사용자 상시 지시). Phase 3 에서 이 표가 실제로 오기를 잡았다(§2.4).

### 2.2 Phase 5 가 반드시 받아야 하는 이월 3건

| # | 내용 | 근거 |
|:-:|---|---|
| 1 | **학부모 → `GuardianStudent` 연결 확인**(Phase 2 Task 5 미착수분) — 완료 조건에 "연결 부재 자녀 조회가 `403`" 포함 | Ruling 117 · `§8` 표 Phase 5 비고 |
| 2 | **`role='staff'` 한정이 조용히 틀리게 되는 지점** — `LoginCommandService.assertStaffStillEmployed` 는 `academy_staff` 만 본다. Phase 5·9 가 `manager`·`guardian`·`student` 레코드를 만들면 같은 축이 생기는데 이름이 `staff` 로 굳어 있다. **일반화 여부를 그때 정한다** | Ruling 148 |
| 3 | **테스트 픽스처의 raw SQL 3곳** — `AcademyDeactivationTest`·T3 테스트·`AcademyOnboardingFlowTest` 가 학생·관계자를 SQL 로 만든다(STU-01 이 Phase 5 소유라 API 부재). **Phase 5 가 실제 등록 경로로 바꾸는 것이 맞다** | T2·T3·T4 보고서 |

### 2.3 Phase 4~14 전체가 상속하는 제약 2건

1. **`AuthFlowIntegrationTest` 의 거부측이 이제 자동 대조된다** — 손 목록(`AccountStatusGateEndpoints`)과 `RequestMappingHandlerMapping` 실측 집합을 **양방향** 대조하고, 프로덕션 핸들러 하한 **24** 를 함께 단언한다. **엔드포인트를 늘리면 이 목록도 함께 늘려야 빌드가 통과한다.** 설계가 그렇다(낡는 것을 실패로 만든다)
2. **계정 상태 게이트가 권한 판정보다 먼저 걸린다** — Ruling 145 로 "지금이 옳다" 확정. 근거 3개가 원장에 있다. **"일관성" 을 이유로 뒤집지 마라**

### 2.4 ⚠ Phase 3 에서 조율자가 낸 실수 2건 (같은 형태를 경계하라)

1. **목표 표의 핸들러 소계를 `11` 로 적었다. 정답은 `12`**(`1+1+2+2+2+2+2`). **T2 가 인용을 사실로 믿지 않고 직접 세어 잡았다.** 그 값이 목표 6 의 판정 근거였으므로 그대로 뒀으면 핸들러 하나가 조용히 검사에서 빠졌다
2. **테스트가 도는 중에 `docker exec` 로 DB 를 만들고 지웠다** → Testcontainers 컨테이너 연결이 끊겨 `initializationError` 1건. **코드 결함으로 오진할 뻔했다.** 실패 예외가 임시 컨테이너 포트(`localhost:51057/test`)를 가리켜 환경 문제로 특정됐다

**둘 다 "동시 실행 중의 실패 목록은 증거 능력이 부재하다" 와 "인용은 사실이 아니다" 를 재확인한 사례다.**

### 2.5 미해결 — Phase 3 소관 아님

| 내용 | 소유 |
|---|---|
| `AccountStatusGateInterceptorTest.countAnnotatedMethods` 가 **완전 수식 애너테이션을 세지 못한다**(`line.equals("@AllowedWhenPending")` 로 줄 전체 일치). 그 형태로 심은 변형이 통과했다 | Phase 2 산출물 — 고치면 Phase 2 판정 기준이 움직여 **별도 단위** |
| `AccountStatusGateEndpoints.productionEndpoints` 의 패키지 필터가 **규칙에 기댄다**(`src.backend..controller`). 프로덕션 컨트롤러가 그 밖에 놓이면 놓치는데, 개수 단언 24 가 그때 실패하도록 걸려 있다 — **단 24 를 함께 고치는 사람이면 통과** | Phase 4 이후 |
| `role='staff'` 한정이 **어떤 단언으로도 고정되지 않는다**(제거해도 실패 0건, 2회 독립 재현). 질의 회피 장치라 검증하려면 Hibernate 질의 횟수 계측 인프라가 필요 | 범위 밖 |
| `StaffApprovalConcurrencyTest`·`AcademyStaffQuotaConcurrencyTest` 가 **시간에 기댄다**(고정 대기). 뒷정리는 `try/finally` 로 보장됨 | 관측되면 |

## 3. 이 Phase 의 Ruling (138~148)

전문은 `progress.md`. **다음 세션이 모르면 손해 보는 것만** 추린다.

| # | 내용 |
|:-:|---|
| 139 | ⚠ **차단 결함** — `uk_academy_staff_academy` 가 전체 UNIQUE 라 **관계자 교체가 영구 불가**였다. partial UNIQUE(`WHERE status='active'`)로 정정 |
| 140 | 학원 코드 = 혼동 문자(`I O 0 1`) 뺀 **대문자 영숫자 8자 난수** + 충돌 시 재생성(상한 있음) |
| 142 | `user_count` 는 **`status IN ('active','blocked')`**. 원칙은 "소속이 확정됐고 아직 종료되지 않은 것" — `blocked` 는 일시 정지이지 종료가 아니다 |
| 143 | ⚠ **퇴사 관계자의 재로그인을 막는다.** `AUTH_STAFF_INACTIVE`(403) 신설. **에러 코드가 없는 것은 사양이 "금지" 한 것이 아니라 "미완" 이다** |
| 144 | `@AcademyScopeExempt` 는 **개별 표시 유지.** `/admin` 으로 묶으면 그 표시가 다음 조회를 자동으로 덮어 진짜 누락과 구별되지 않는다 |
| 145 | **계정 상태 게이트가 권한보다 먼저**가 옳다(§2.3 참조) |
| 147 | `SIGNUP_TARGET_BLOCKED`(**409**) 신설 — 승인 **대상**이 차단된 경우. 403 이 아닌 이유는 요청 주체가 인가돼 있고 막는 것이 **대상 자원의 상태**여서 |

## 4. 환경

- **Docker 3종 기동 상태 유지**(postgres·redis·kafka). ⚠ **`docker compose down` 을 하지 않았다** — 다음 세션이 그대로 쓴다
- 병렬 작업용 DB 가 남아 있다 — `schoolbus_rt1`·`_t2`·`_t3` 등. **공유 Postgres 의 `max_connections` 가 100 이라 에이전트 3개 이상이 붙으면 `sorry, too many clients already`** 가 난다. **코드 결함이 아니다**
- **워크트리 정리** — Phase 3 것 3개는 **회수를 조상 관계로 확인한 뒤 제거**(`git merge-base --is-ancestor`). ⚠ **Phase 2 것 6개(`wt-p2t1`~`wt-p2t5`·`wt-p2t3b`)가 남아 있고 일부러 지우지 않았다** — Phase 2 는 **cherry-pick 으로 회수**해 브랜치 tip 이 HEAD 의 조상이 아니다. 파일은 HEAD 에 존재하나 **"전부 회수됐다" 를 기계로 증명할 수단이 부재**해, 되돌릴 수 없는 삭제를 하지 않았다. 정리하려면 브랜치별로 내용 대조를 먼저 하라
- `caffeinate -dimsu` 로 절전 억제 중. ⚠ **중단 시 `pkill caffeinate`.** `-i` 만으로는 부족했다(세션 중 실제로 절전에 들어가 에이전트 1개가 응답 중 끊겼다)
