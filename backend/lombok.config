# Lombok 설정 — backend 모듈 루트.
#
# stopBubbling: Lombok 은 소스 파일 위치에서 파일시스템 루트까지 거슬러 올라가며 lombok.config 를
# 합친다. 현재 상위(저장소 루트·홈·/)에는 아무 설정도 부재하나, 나중에 누군가 상위에 파일을 두면
# 이 모듈의 생성 코드가 조용히 달라진다. 여기서 끊어 backend 의 생성 규칙을 이 파일 하나로 고정한다.
config.stopBubbling = true

# copyableAnnotations: @RequiredArgsConstructor 가 만드는 생성자 파라미터로 복사할 애너테이션.
# final 필드는 필드 주입이 불가능하므로 @Value 가 생성자 파라미터까지 따라가야 값이 주입된다.
#
# 아래 두 줄을 지우면 컴파일은 통과하고 기동이 실패한다 — 실측(주석 처리 후 build --rerun-tasks):
# loginCommandService 생성에서 NoSuchBeanDefinitionException: No qualifying bean of type 'long'.
# @Value 를 잃은 생성자 파라미터를 Spring 이 타입으로 autowire 하려 들기 때문이고, 컨텍스트가
# 뜨지 못해 298개 중 76개(20개 클래스)가 실패한다. 값이 0/null 로 조용히 들어가지는 않는다.
#
# ⚠ Gradle 은 이 파일을 compileJava 의 입력으로 추적하지 않는다(실측: 파일만 고치면
# compileJava UP-TO-DATE). 이 파일을 고쳤으면 반드시 --rerun-tasks 로 다시 컴파일한다.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
