# 백엔드 구현 계획 색인

## 2026-08-02 · MVP 확장 (선탑자 · 학부모 · 배차 시뮬레이션 · 관제 상세)

**먼저 읽을 것:** [`docs/plans/2026-08-02-mvp-확장-OVERVIEW.md`](../../../docs/plans/2026-08-02-mvp-확장-OVERVIEW.md)
결정(D-A~D-J) · 불변조건(I-1~I-5) · API 계약 · DTO 정의가 **전부 거기 있다.** 아래 문서들은 그것을 참조만 한다.

| 문서 | 태스크 | 성격 |
|---|---|---|
| [BE-P1 기반](./2026-08-02-mvp-확장-BE-P1-기반.md) | `BE-1` 스키마·시드 · `BE-2` 엔티티 · `BE-3` 승하차 권한 재배치 · `BE-11` 회원가입 권한상승 차단 | **순차 필수.** 다른 모든 백엔드 작업의 전제 |
| [BE-P2 엔진](./2026-08-02-mvp-확장-BE-P2-엔진.md) | `BE-4` 노선 시뮬레이션 엔진 · `BE-5` 시뮬레이션 API | 이번 확장의 **중심**. 요구 8과 P2가 공유 |
| [BE-P3 관제·학부모](./2026-08-02-mvp-확장-BE-P3-관제-학부모.md) | `BE-6` 버스 상세 · `BE-7` 배차 선탑자 · `BE-8` 위치 실시간 · `BE-9` 학부모 버스위치 · `BE-10` 위치변경 요청 | `BE-2` 이후 병렬 가능 (`BE-10`만 `BE-4` 선행) |

### 실행 순서

```
BE-11 ─────────────────────────────────────────────┐ (독립, 언제든)
BE-1 → BE-2 → BE-3                                 │
              ├→ BE-4 → BE-5                       │
              ├→ BE-6                              ├→ 문서 갱신(DOC-*)
              ├→ BE-7                              │
              └→ BE-8 → BE-9                       │
                 BE-4 + BE-1 → BE-10 ──────────────┘
```

### ⚠️ 이 확장에서 반드시 지킬 것

- **`V1__init_schema.sql` 을 직접 수정한다.** 새 마이그레이션(V6…)을 만들지 않는다 → 반영하려면 `docker compose down` 후 `up`. `stop`/`start` 는 Flyway 체크섬 검증에 걸려 앱이 뜨지 않는다.
- **Docker 는 직접 실행하지 않는다.** 사용자에게 요청한다.
- **기사에게서 뺏는 것은 기록 권한뿐이다.** 운행 세션 시작·종료와 버스 위치 보고는 `DRIVER` 유지.
- **시뮬레이션은 어떤 행도 저장하지 않는다.** 저장은 `apply` 계열에서만.

---

## 이전 계획

- [`PROJECT_MASTER_PLAN.md`](../PROJECT_MASTER_PLAN.md) — 백엔드 전체 진행 추적·모듈 백로그 (§11~12)
