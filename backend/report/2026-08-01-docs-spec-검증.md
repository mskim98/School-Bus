# docs/ 3종 SoT 검증 보고 (2026-08-01)

대상 문서: `docs/PRODUCT_SPEC.md`(493행) · `docs/ARCHITECTURE.md`(1,019행) · `docs/USER_FLOWS.md`(671행)
검증한 단정(수치·경로·상태표기) 약 40건, 대조한 코드/설정 파일 50여 개
(백엔드 14개 컨트롤러 전수 + 엔티티 16개 전수 + `V1__init_schema.sql`/`V2__seed_data.sql`/`application.yml`/`build.gradle`/`docker-compose.yml` 전문 + 프론트 `data/*_repository.dart` 11개 + `app_router.dart`/`api_client.dart`).

| 파일:라인 | 담당 | 유형 | 심각도 | 근거·제안 |
|---|---|---|---|---|
| docs/USER_FLOWS.md:368 | docs-drift-auditor | 값 불일치 | 중요 | "프론트가 호출하는 REST는 22개, 백엔드 매핑은 약 64개, 사용률 약 34%"라 하나 `PRODUCT_SPEC.md:273-274`, `ARCHITECTURE.md:42,997`은 "68개(14 controllers)+STOMP1, 사용률 약 32%"로 서로 다르다. `@GetMapping\|@PostMapping\|@PatchMapping\|@DeleteMapping` 직접 카운트 결과 68건으로 PRODUCT_SPEC/ARCHITECTURE 쪽이 맞다 — USER_FLOWS.md의 "64개/34%"가 오기. |
| docs/ARCHITECTURE.md:364,993 | docs-drift-auditor | 값 불일치 | 중요 | "@PreAuthorize 실측 61개소(클래스 레벨 3+메서드 레벨 58)"라 하나, `grep -rn "@PreAuthorize"` 원시 매치 64건 중 9건은 Javadoc/주석(`location/controller/LocationController.java:32`, `bus/controller/BusController.java:48`, `drivesession/controller/DriveSessionController.java:29`, `rideevent/controller/RideEventController.java:31`, `routing/controller/RoutingController.java:34`, `global/tenant/TenantGuard.java:9`, `global/security/SecurityConfig.java:29`, `global/security/AuthUser.java:20`, `global/error/GlobalExceptionHandler.java:29`)라 실제 애너테이션이 아니다. 실제 애너테이션 수는 55개소(클래스 레벨 3 — `StudentController.java:36`/`BusController.java:35`/`MemberController.java:32` — + 메서드 레벨 52). 클래스 레벨 3은 정확, 메서드 레벨/합계가 틀림. |
| docs/ARCHITECTURE.md:435,447 | docs-drift-auditor | 값 불일치 | 경미 | "TenantGuard 24개소"라 하나 `TenantGuard.resolveTenantId(` 실제 호출은 26건이다(예: `backend/src/main/java/src/backend/routing/command/RoutingCommandService.java:91,129,139`, `student/command/StudentCommandService.java:67,141`). |
| docs/ARCHITECTURE.md:447 | docs-drift-auditor | 누락 | 경미 | "같은 조건을 인라인으로 다시 쓴 4개소(attendance·schedule·sos의 승인 계열, tenant 상세)"라 하나, 동일 패턴(`isPlatformAdmin()`+`belongsToTenant(...)`)이 `backend/src/main/java/src/backend/rideevent/command/RideEventCommandService.java:116`에도 있어 실제는 5개소. |

## 일치 확인 (압축)

엔티티 16개(+`BaseTimeEntity` MappedSuperclass 1)/테이블 16개/FK 13/인덱스 2/유니크 4, REST 엔드포인트 68개(컨트롤러 14)+STOMP 1(`LocationSocketController.java:28`, 역할 검사 없음도 확인), 프론트 실사용 엔드포인트 22개/사용률 32%, 마이그레이션 파일 5개, docker-compose 서비스 6개/영속 볼륨 0개, 스케줄러 4종 주기(3s/10s+30s grace/15s/30s)·JWT(15분/14일)·알림 임계값(10/5/3분) 전부 `application.yml`·`NotificationThresholds.java`와 일치, `RideType`(3)·`NotificationType`(10) enum 순서·값 일치, `@DeleteMapping` 0건·`@KafkaListener` 16건(컨슈머 3클래스)·`NewTopic` 빈 부재·Redis 캐싱 애너테이션 부재 모두 확인, 시드 데이터(테넌트3/계정5/노선3/정류장3/버스3/학생6/보호자연결2) 및 테스트 파일 21개 일치, 프론트 라우트 9개(splash1/login1/기사2/관리자5, `app_router.dart`) 일치, `ApiClient`에 put/delete 없음(`api_client.dart`) 및 쓰기 호출 7종 일치, `build.gradle`에 actuator 의존성 부재 일치, `RoutePlanStop`이 `BaseTimeEntity` 미상속·`route_plan_stop` 테이블에 `created_at`/`updated_at` 없음 일치, `SosTriggerRequest.lat/lng`에 `@NotNull` 없음 일치, `RideEventCommandService.record()`에 순서·중복 검증 로직 없음 일치, `WebClientConfig.java` 타임아웃(connect 3000ms/response·read·write 5s) 정확 일치, `SecurityConfig.java:41` `@EnableMethodSecurity` 인용 정확.
