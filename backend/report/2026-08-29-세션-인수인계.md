# 세션 인수인계 — 2026-08-29 (Phase 6 진행 중)

**이 문서는 포인터다.** 사실의 정본은 `docs/IMPLEMENTATION_PLAN.md §8`(진행 표)과
`.superpowers/sdd/IMPLEMENTATION_PLAN/progress.md`(작업 원장·Ruling)이며, 여기서는 **어디를 보라**만 가리킨다.

## 1. 지금 상태 한 줄

`feat/baraeda-rebuild` tip **`b437c13`** — **Phase 6 진행 중(🟡)**. 목표 **11항 중 9항 회수 완료**,
남은 것은 **목표 9(동승자 자동 배정, T6 미착수)**와 **목표 11(전체 실증)**.

## 2. 다음 세션이 **먼저 읽을 것** — 순서대로

0. ⚠ **`.claude/PROJECT_NOTES.md`** — **에이전트를 띄울 세션은 이것부터.**
   명명 규칙(`name` 필수) · 리뷰어 입력 diff 규격 · `caffeinate` · Gradle 데몬 공유.
   **이 세션이 안 읽어 규칙 4건을 어겼다**
1. `docs/IMPLEMENTATION_PLAN.md §8` Phase 6 행 · Phase 6 절
2. **`progress.md` 의 "Phase 6 착수 전 판정" 절부터 끝까지** — Ruling **178~185** 와 태스크별 판정
3. **`p6-goal-table.md`** — 목표 11항(+ 5-a). 이것이 완료 판정의 정본
4. **`p6-port-contracts.md`** — 포트 계약. T6 이 §3(`AttendantAssigner`)을 쓴다

## 3. 즉시 해야 할 것 3가지

### ① ⚠ 전체 묶음 단독 실증 (목표 11) — **이 세션이 못 했다**

마지막 실행이 **734 tests, 418 failed** 로 끝났으나 **코드 결함이 아니라 환경 문제**다.
근거는 `progress.md` 맨 뒤 "조율자 절차 위반 5번째" 절.

| 증상 | 원인 |
|---|---|
| `FlywayValidateException` 45건 | 공유 DB **`schoolbus`** 를 썼다 (낡은 상태) |
| `Connection is not available` (`SQLState: 08001`) | **T4 리뷰가 동시에 도는 중**이었다 |

**반드시 이렇게 돌려라** — 신규 DB 생성 후 **동시 실행 좌석 0개**에서 1회.

```bash
docker exec school-bus-postgres-1 psql -U schoolbus -d schoolbus -c "CREATE DATABASE p6final;"
cd backend && ./gradlew test -PtestDbUrl=jdbc:postgresql://localhost:5432/p6final
```

### ② 조율자 직접 편집 2건이 **리뷰를 건너뛴 채로 있다** (규칙 §10)

- **`e91dcc6`** `GeoPoint` — T2 리뷰가 근거를 지지했으나 **"T4 가 붙으면 재확인 필요"** 조건부.
  T4 가 "만나지 않음" 을 재확인했고 **T4 리뷰 판정은 미수령**
- **`b437c13`** 병합 충돌 해소 2파일(`ErrorCode`·`StopRepository`) — **컴파일만 통과, 리뷰 미경유**

**다음 리뷰 발주의 판정 대상에 이 두 커밋을 명시하라.**

### ③ `API_SPEC` 갱신 (**Ruling 185**) — 미착수

T4·T6 이 동시에 읽는 SoT 라 미뤘다. **좌석이 비었을 때 한다.**

| 절 | 낡은 문면 |
|---|---|
| §5.9 (1319-1327행) | `[조정 중]` 태그 · "최적화 트리거·조건은 배차 정책 확정 후" · "고유 에러 부재" **3곳** |
| §8 | `DUPLICATE_ROUTE`(409) · `ROUTE_NOT_FOUND`(404) · `MAP_ROUTE_UNAVAILABLE`(503) 등재 여부 확인 |
| §10 표 2103행 | `GET /staff/routes` 행 |
| §10 표 **2104-2105행** | **이 태스크 이전부터 낡음** — Ruling 153 이 해제했는데 표만 안 지워짐 |

⚠ **§6.1 이 경고한 형태다. 표 전체를 정본과 대조하라** — 하나가 낡으면 나머지도 낡아 있다.

## 4. 미수령 판정 1건 — **T4 게이트 리뷰**

좌석 `p6-t4-review-gate-opus` 가 커밋 **`7da8012`** 를 판정 중이었다.
워크트리 `/Users/mskim/Desktop/PJ/School-Bus-p6-t4rv` · DB `p6t4rv`.

**T4 는 아직 회수되지 않았다**(브랜치 `p6-t4`, tip `7da8012`).
판정을 받으면 수정 라운드 또는 회수로 진행한다. 발주 내용은 `progress.md` 의
"P6-T4 구현 완료" 절에 판정 대기 3건(A·B·C)과 자기 신고가 정리돼 있다.

⚠ **판정이 안 오면** — 워크트리의 `backend/build/test-results/test/` 타임스탬프를 보고
작업 흔적이 있으면 **재실행이 아니라 재전송**을 요청하라(`progress.md` "규칙 충돌 발견" 절).

## 5. 남은 태스크 — T6 (목표 9)

브리프가 **이미 준비돼 있다** — `.superpowers/sdd/IMPLEMENTATION_PLAN/p6-task-6-brief.md`
(이 Phase 의 실측 교훈 5가지 + 조율자 판정 2건이 실려 있다).

**선행은 T4 회수**다(⑤가 ④의 `estDurationMin` 을 입력으로 받는다).

## 6. 회수 현황

| 태스크 | 목표 | 리뷰 | 회수 커밋 |
|---|---|:-:|---|
| T2 최적화 엔진 | 7·10 | 2회 | `c93be59` |
| T3 지도 어댑터·격벽 | 3·4·5·5-a | 2회 | `f943177` |
| T1 자문 잠금 | 8 | **3회** | `80e32e1` |
| T5 노선 CRUD | 1 | 2회 | `b437c13` |
| **T4 파이프라인** | 2·6 | **판정 미수령** | **미회수** (`p6-t4` tip `7da8012`) |
| **T6 동승자 배정** | 9 | — | **미착수** |

## 7. 환경

- Docker 는 **켜 둔 채로 둔다**. 다른 프로젝트 스택(`dev-db`·`dev-redis`·`dev-kafka`)이 포트를
  선점하면 그것들을 `docker stop` 하고 `docker compose start postgres redis kafka`
- **동시 JVM 은 3개까지.** 테스트 DB 는 좌석마다 `-PtestDbUrl` 로 가른다
- ⚠ **리뷰가 도는 동안 조율자는 같은 트리에서 `./gradlew test` 를 돌리지 마라**(§9). 이 세션이 어겼다
- 남은 워크트리 — `p6-t4` · `p6-t4rv` · `p6-t5` · `p6-t5rv` · `p6-t5rv2`.
  **회수 확인 후 제거하되 브랜치는 보존**한다
- 남은 테스트 DB — `p6t4` · `p6t4rv` · `p6t5` · `p6t5rv` · `p6t5rv2`

## 8. 이 세션의 대표 교훈 — **"단언·가드는 있는데 안 문다" 가 네 번**

| # | 태스크 | 형태 |
|:-:|---|---|
| 1 | T2 | 단언 3개인데 **좌표 배치** 때문에 1개만 물음 |
| 2 | T5 | 가드 2개인데 **앞 가드가 뒤를 가림** |
| 3 | T5 | `ORDER BY seq` 계약 — 시험이 **행을 순번대로 넣어** 통과 |
| 4 | T4 | 조건 2개가 **서로를 가림** — 실제 강제자는 **제3의 코드**(호출부) |

**네 번 다 판별 수단은 변형을 심어 실패 건수를 세는 것 하나뿐이었다.**
**다음 Phase 브리프에 반드시 싣는다.**

**그리고 "근거는 틀렸는데 결론은 맞다" 가 네 번** — 리뷰↔구현이 서로를 실측으로 정정했다.
**주장을 옮길 때 등급(관측/가능성/확정)을 함께 옮겨라** — 이 세션이 남의 "가능성" 을
원장에 확정으로 적었다가 반증됐다.

## 9. 전역 규칙에 올린 것 1건

**`~/.claude/rules/parallel-agents-git.md §1.1`** — `git add` 부분 실패로 **한 라운드 작업이
통째로 사라지는 기제**(exit 128 → `2>/dev/null` 이 가림 → `RM` 오독 → `git checkout --` 이 지움).
독립 재현 완료. **`RM` 은 "rename 은 담겼고 수정분은 아직" 이라는 뜻이다.**
