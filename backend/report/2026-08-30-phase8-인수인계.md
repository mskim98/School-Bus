# Phase 8 완료 기록 (2026-08-31)

⚠ **이 파일은 세션 중단용 인수인계였고 Phase 8 이 끝나 완료 기록으로 대체했다.**
**판정 근거의 정본은 `docs/IMPLEMENTATION_PLAN.md` §8 진행 추적 표 Phase 8 행**이고,
목표 표는 `.superpowers/sdd/IMPLEMENTATION_PLAN/p8-goal-table.md` 다. 여기서는 요지만 가리킨다.

| 항목 | 값 |
|---|---|
| 브랜치 · 최종 커밋 | `feat/baraeda-rebuild` · **`1450464`** |
| 최종 실측 | **150클래스 891테스트 실패 1 · 오류 0 · 건너뜀 3** (새 컨테이너 · 신규 DB · 동시 실행 0) |
| 프로덕션 핸들러 | **65** (56 → +9, 직접 계수) |
| 테이블 | **40** (`run_forced_addition` 신설) |
| postgres 재시작 | 전후 **1 → 1** |

**실패 1건** — `AssignmentConcurrencyTest` 의 `TimeoutException`. 부하 의존으로 분류했다
(단독 3클래스 재실행 **15초 전건 통과** · `too many clients` 0 · 크래시 0).
⚠ **"정상" 이 아니다** — 두 스레드의 순서를 강제할 수단이 없어 "느린 호스트" 와 "잠금이 깨진 구현" 이
같은 실패로 나타난다. Phase 5·8 에 걸쳐 세 번 관측됐고 **Phase 9 이월 4건 중 하나**로 등재했다.

## 다음 세션이 읽을 것

1. `docs/IMPLEMENTATION_PLAN.md` **§8 표 Phase 8 행** — 판정·회수 커밋·리뷰가 잡은 결함
2. 같은 문서 **Phase 9 절 앞머리** — 이월 4건과 "값의 갈래를 전부 검사하는가" 질문
3. 🛑 **Phase 9 착수 전 사용자 확인** — `RUN-08` 외부 내비 앱의 경유지 개수 상한(오픈 이슈 V)이
   미실측이다. 값 없이 만들면 앱이 경로를 조용히 잘라 **기사가 다른 경로로 간다** — 안전 문제다

## 정리 상태

워크트리 0개 · `p8-*` 브랜치 정리 완료 · 작업 공간 깨끗 · **DB 는 `schoolbus` 만** ·
Docker 3개(postgres·redis·kafka) 기동 중 · `caffeinate` 해제됨.
